# CodeRAG 开发清单（已完成 / 未完成）

> 版本：v1.3　|　更新日期：2026-07-27　|　维护：Claude
> 配套：[开发实施计划与方案.md](./开发实施计划与方案.md)、[待确认问题清单.md](./待确认问题清单.md)
>
> **图例**：`[x]` 已完成　`[ ]` 未完成　`[~]` 进行中　`[!]` 阻塞（见待确认问题）
> **状态约定**：每完成一项就把 `[ ]` 改成 `[x]` 并补上完成日期。这是本项目的"真实进度账本"。

---

## 0. 规划与文档（已完成）

- [x] 2026-07-24　通读 7 份设计文档，理解全貌
- [x] 2026-07-24　识别文档间冲突，产出架构默认裁决（见计划 §3）
- [x] 2026-07-24　编写《开发实施计划与方案.md》
- [x] 2026-07-24　编写《开发清单.md》（本文件）
- [x] 2026-07-24　编写《待确认问题清单.md》

---

## Phase 0 — 基础设施与脚手架 ✅

> **架构（2026-07-25 调整）**：后端/前端/model_server 在本地 host 跑，**Docker 只跑 infra**（PG/Redis/MinIO/ES/Milvus/etcd/attu）以降内存。compose 已 infra-only（无 backend/frontend/nginx/model_server）。

- [x] 仓库初始化：monorepo 目录（`backend/ frontend/ model_server/ docker/ docs/ data/`）+ git init
- [x] `docker/docker-compose.yml`（infra-only）：postgres16 + redis7 + minio + elasticsearch8 + milvus + etcd(bitnami) + attu
- [x] Milvus 全套（etcd + minio + milvus + attu）镜像就绪并 healthy（**关键修复**：milvus 须配 `MINIO_ACCESS_KEY_ID/SECRET` 匹配 MinIO 凭据，否则 crashloop）
- [x] 不引入 Neo4j（图用 PG call_graph + DGL/PyG）
- [x] `backend/` 骨架：FastAPI + `core/{config,logging,deps}` + `/health` 多组件探活
- [x] `backend/` 依赖（uv）：FastAPI/Uvicorn/sse-starlette/SQLAlchemy/asyncpg/psycopg/Alembic/redis/pydantic-settings/loguru/httpx/jose/passlib + tree-sitter(-java)/jieba/elasticsearch(8.x)/pymilvus
- [x] Alembic 初始化 + 建表：16 张表（含 §18 回滚 + PDF/Word 扩展 + `doc_resources`），`alembic upgrade head` 成功；pg_trgm/pgcrypto + GIN/部分索引生效
- [x] `frontend/` 骨架：Vite + React18 + TS + AntD5 + Router6 + Zustand + axios + SSE 客户端（`pnpm install` + `typecheck` + `build` 通过）
- [x] 前端三栏布局壳（深色 + 橙色主题）
- [x] `Makefile` + `docker/nginx/nginx.conf`（开发态直连 vite，nginx 配置备用）
- [ ] `.github/workflows/`：lint + 测试 + 镜像构建（待补）
- [x] **验收**：✅ infra `compose up` 7 服务全 healthy；✅ 本地 backend `/health` 5 组件全绿；✅ `alembic upgrade head`；✅ 前端 `build`。（GPU 透传 probe 亦通过，但已决定嵌入走 API，故未用）

> **环境备忘**：中文 Windows，Docker Desktop 已配国内镜像加速（`daemon.json`：1ms.run / xuanyuan.me / daocloud / 1panel / nju / rat.dev）；Git Bash 调用 docker 需把 `C:\Program Files\Docker\Docker\resources\bin` 加入 PATH。


---

## Phase 1 — 基础 RAG 主干

### 1.1 数据管道：解析
- [x] `pipeline/parsing/code_parser.py`：tree-sitter Java AST（包/导入/类/方法/签名/参数/返回/Javadoc/注解/行号/调用表达式/implements/extends）✅ 已用示例验证
- [x] `pipeline/parsing/markdown_parser.py`：行级解析 → 标题路径 + `CODE_ANCHOR` 识别 ✅
- [x] `pipeline/parsing/doc_element.py`：统一中间结构（ParsedCodeFile/CodeClass/CodeMethod/DocElement/ChunkSpec）
- [ ] `pipeline/parsing/router.py`：按扩展名路由（Phase 1.5 多格式统一入口）

### 1.2 数据管道：切片
- [x] `chunking/code_chunker.py`：方法级默认 + 小文件(<200行)合并 + 超长(>1024token)二次切分 + 上下文前缀（§4.1）✅
- [x] `chunking/doc_chunker.py`：H2/H3 章节级 + heading_path + 大代码块独立 + 超大章节段落窗口切分（§4.2）✅
- [x] 切片 ID 命名规范（§4.3）：`code_{Class}_{method}_{hash8}` / `doc_{hash8}_{order}`

### 1.3 元数据与关联
- [x] `pipeline/metadata.py`：content_hash(SHA256) + 锚点 key(ClassName.methodName) + keywords(标识符 camelCase + 文档词) + token 估算 ✅
- [x] `pipeline/relations.py`：**锚点精确匹配 → DOC_TO_CODE/CODE_TO_DOC + anchor_mappings** ✅（示例：Foo.checkLocalTransaction/Foo.resolveHalfMsg ↔ 文档 3.2 节）
- [x] `call_graph` 构建：调用表达式 → 同类内简单名解析（Phase 1）✅（示例：run→checkLocalTransaction→resolveHalfMsg/sendCheckMessage）
- [ ] `CODE_IMPLEMENTS` / `CODE_EXTENDS` / `CO_FILE` / `CO_MODULE`（留待 Phase 5 图构建；跨类/类型感知调用解析亦后续增强）

### 1.4 向量化与索引
- [x] `pipeline/ingest_code.py` + `ingest_doc.py` + scripts：代码/文档入库编排（含外键引用清理）；✅ 示例验证
- [x] **API 嵌入（文档侧 / 框架一）**：`clients/embedding_client.py`（OpenAI 兼容 `/embeddings`，硅基流动 BGE-M3 1024d，免 GPU）✅
- [x] 2026-07-27　**可切换双框架嵌入**：`EMBEDDING_STRATEGY=unified`（全 BGE-M3，单 collection + kind 过滤）/ `=dual`（[方案一](./嵌入向量方案.md)：代码 CodeBERT 768d 经本地 `model_server` + 文档 BGE-M3，双 collection，统一 reranker 重排）；`ingest_embed`/`query_embed` 调度 + 各路独立降级 ✅
- [x] 2026-07-27　**本地 CodeBERT 服务**：`model_server/main.py`（transformers 加载 microsoft/codebert-base，mask 感知 mean-pool + L2，768d；GPU 优先 CPU 回退）✅
- [x] **Milvus 双布局**：`clients/milvus_client.py`（unified→`coderag_vectors`1024 +kind 字段；dual→`code_vectors`768 / `doc_vectors`1024，HNSW COSINE）✅
- [x] 2026-07-27　入库向量化成功后置 `embedding_synced=True`（修掉"永远 False"瑕疵）✅
- [x] **ES 全文索引**：`clients/es_client.py`（elasticsearch-py 8.x 配 ES8.11）+ 入库索引；bm25:True 实测
- [x] 2026-07-28　**统一索引一致性层 + embedding_synced 补偿**：`pipeline/indexing.py`（embed 文本单一真相源 `embed_text_for` + 批处理 ES/Milvus 写入 + `resync_pending_embeddings` 补偿，编码器禁用 no-op、每批 try/except+可选 commit）；`pipeline/ingest.py` 统一 `ingest_repo`（parse→chunk→relate→embed→index 单入口）；`ingest_code/ingest_doc` 收敛去重；`main.py` lifespan 可选后台补偿循环（`asyncio.to_thread` 卸载，默认关，Celery 留作未来薄封装）；CLI `scripts/ingest_repo.py` + `scripts/resync_embeddings.py`，旧两脚本改薄封装；新增 4 个 Settings（embed_batch_size / ingest_resync_*）；14 个无基础设施单测 ✅
- [ ] Celery 任务化（当前用轻量 asyncio 循环；补偿逻辑已是纯函数 `resync_pending_embeddings`，Celery beat 可薄封装）—— 留待真正需要分布式/重试队列时引入

### 1.5 检索与生成
- [x] `retrieval/query_understanding.py`：Stage 0 检索词提取（英文 camelCase + 中文 jieba 分词）✅
- [x] `retrieval/lexical_search.py`：PG 关键词召回（BM25 占位/降级，code+doc）✅
- [x] `retrieval/bm25_search.py`：路径 B ES BM25 召回（中文 terms on keywords + 英文 match content）✅
- [x] `retrieval/graph_traverse.py`：路径 D 图遍历召回（call_graph + chunk_relations BFS）✅
- [x] `retrieval/vector_search.py`：路径 A 向量召回（Milvus + API 嵌入）✅
- [x] `retrieval/pipeline.py`：Stage0→A/B/D 三路召回→融合打分（全含降级）✅ 实测 lexical6+vector7+graph1
- [x] `clients/llm_client.py` + `services/chat_service.py`：LLM 流式 + Prompt 组装 ✅（DeepSeek-v4-flash 真实生成，带 [代码]/[文档] 引用）
- [x] SSE 流式（retrieval / citation / token / done 事件）✅

### 1.6 API（智能问答模块 6 接口）
- [x] 2026-07-25　`POST /api/v1/chat/completions`（SSE，含 conversation 落库事件）✅
- [x] 2026-07-25　`GET /api/v1/chat/conversations`（列表/分页/agent 过滤）+ `/{id}`（详情+消息）✅
- [x] 2026-07-25　`GET /api/v1/chat/messages/{id}/retrieval`（stage1/2/3 漏斗，读 retrieval_logs）✅
- [x] 2026-07-25　`POST /api/v1/chat/suggestions`（LLM 追问）+ `messages/{id}/feedback`（落 retrieval_logs，供 LTR）✅

### 1.7 前端 P0
- [x] 智能问答页骨架（三栏 + 输入区 + Agent 选择器，深色橙主题）✅
- [x] **对话流接线 SSE**（useChat hook + 流式渲染回答 + 引用卡片 + 检索信息 + 停止/清空）✅ 经 vite 代理实测：检索/引用/token 流式正常
- [x] 2026-07-25　**会话侧边栏**（列表/新建/切换/历史回显，conversations API）✅
- [x] 2026-07-25　**检索详情抽屉**（点消息展开 stage1 召回+RRF / stage2 粗排 / stage3 精排候选与分数）✅
- [x] 2026-07-25　**追问建议**（3 条气泡可点发）+ **消息反馈** 👍👎 ✅
- [x] 2026-07-25　代码/文档引用卡片（`[代码] Class.method` / `[文档]` 章节面包屑 + 精排分数）✅
- [ ] 右侧常驻上下文面板（引用来源/关联关系/调用链）—— 当前用检索详情抽屉替代；常驻面板后续
- [ ] 代码引用块编辑器级语法高亮（行号/高亮）—— 当前为引用卡片标签，编辑器级高亮后续

- [x] **验收**：✅ **完整 RAG 闭环 + 三路召回 + 精排 + 会话持久化 + 前端**（React 问答页 → SSE → **ES BM25 + Milvus 向量 + 图遍历** 三路召回 → **RRF 融合** → **bge-reranker-v2-m3 精排** → DeepSeek 流式生成 → 流式渲染 + 引用卡片 + 检索漏斗 + 会话侧边栏 + 检索详情抽屉 + 追问/反馈）。实测 `lexical6 + vector7 + graph1 → merged7`；精排 relevance 0.99/0.66…；会话/消息/检索详情/反馈/追问 API 全通。

---

## Phase 1.5 — PDF / Word 多模态解析（与 P2/P3 可并行）

### 1.5a 文本解析 + 切片
- [x] 2026-07-28　**`pdf_parser.py`（PyMuPDF/fitz）**：`get_text("dict")` 取 span 字号/加粗；正文字号=众数（并列取较小者）；字号倍率判 H1/H2/H3；同页正文行合并；每元素带 `page_number`；页面可抽文本<20 字符判扫描页→`ocr_required/PARTIAL`（不做 OCR）✅
- [x] 2026-07-28　**`word_parser.py`（python-docx）**：按 `paragraph.style.name`（Title/Heading N）出 HEADING，余为 PARAGRAPH；表格按行渲染为 TABLE 文本元素（结构化 JSON 留 1.5c）✅
- [x] 2026-07-28　**`txt_parser.py`**：UTF-8→gb18030→latin-1 解码阶梯（中文 Windows GBK）+ 空行分段 ✅
- [x] 2026-07-28　**`router.py` 唯一真相源**：`DOC_FORMAT_EXTS`/`EXT_KIND`（.java→code，.md/.pdf/.docx/.txt/...→doc），`parse_doc(data,ext,file_path)→(elements,ParseMeta)` 分发；`ingest.DEFAULT_EXTS` 与 `sync_git._EXT_KIND` 共用 ✅
- [x] 2026-07-28　**PDF 标题层级推断**（字号倍率 1.5/1.3/1.15 + 加粗）+ 跨页 `page_number` 透传 ✅
- [x] 2026-07-28　**统一切片引擎接入多格式**：`DocChunkSpec` 增 `page_number`/`chunk_content_type`；`chunk_doc_elements` 透传；`ingest_doc.ingest_doc_file` 二进制安全读盘→`parse_doc`→`ParseMeta` 填 `doc_files`（替换硬编码 markdown）→ ES/Milvus 不变；markdown 走薄包装、`chunk_id` 稳定 ✅
- [x] 2026-07-28　**新增依赖** `pymupdf>=1.24` + `python-docx>=1.1`（Tsinghua 镜像）；增量同步经 `ingest_doc_file` **免费**支持新格式 ✅
- [x] 2026-07-28　**验收**：15 单测（解析器/路由/chunk 透传/入库 e2e，fitz+python-docx 内存生成夹具）+ ruff；真实多格式 e2e（`data/repo/multiformat`）—— `guide.pdf`(2 页→2 个 page-aware chunk, format=pdf/total_pages=2/pymupdf)、`spec.docx`(format=docx/total_tables=1)、`notes.txt`(format=txt)，全部 `parse_status=COMPLETED`、`embedding_synced=true`、ES 命中 ✅
- [ ] `ocr.py`（PaddleOCR，扫描件实际 OCR）—— 1.5a 仅检测标记 `ocr_required`，**延迟**
- [ ] pdfplumber / mammoth —— 1.5a 未用（PyMuPDF 单库够文本解析），**延迟**

### 1.5b 图片处理管道
- [x] 2026-07-29　**图片提取**：PDF `page.get_images`+`doc.extract_image(xref)`；Word 遍历 `document.part.rels` 取 image 关系 → blob。字节入 `DocElement.metadata`（ext/width/height/page）✅
- [x] 2026-07-29　**MinIO 存原图+缩略图**：Pillow `thumbnail`（240px）生成 PNG 缩略图；原图 `images/{uuid}.{ext}`、缩略图 `images/{uuid}_thumb.png` ✅
- [x] 2026-07-29　**OCR 描述生成**（**用户指示：图像用 OCR**，弃多模态 LLM）：`parsing/ocr.py` 用 `rapidocr-onnxruntime`（PaddleOCR ONNX 移植，轻量免 paddlepaddle），懒加载+**任一失败优雅降级为空串**（图片仍入库按上下文索引）✅
- [x] 2026-07-29　**图片去重 + 无效过滤**：aHash（8×8 平均哈希，`tobytes`）同文档去重；<50px / 纯色空白过滤 ✅
- [x] 2026-07-29　**图片切片**：`pipeline/images.process_images` → image DocChunkSpec（`chunk_content_type=image`，content=OCR 文本+caption+前后 200 字上下文，`image_url/thumbnail/description/width/height/context_before/after`）+ `doc_resources` 行；`ingest_doc` 接入并入 ES/Milvus 索引。`DocChunkSpec`+`_to_orm` 透传 image 列（schema 已就绪，**无新迁移**）✅
- [x] 2026-07-29　**验收**：5 单测（过滤/去重/缩略图/process_images/OCR 降级，全 mock）+ 全量 94 测 + ruff 通过 ✅；**真实 e2e 通过**——`img.pdf`（Pillow 画 "ORDER 12345" 文本图→fitz 嵌入）上传→image chunk `image_description="ORDER 12345"`（OCR 正确识别）+ `doc_resources`（原图 5907B PNG+缩略图入 MinIO 可取回）+ ES 按 "ORDER" 命中（OCR 文本被索引）✅
- [ ] `DOC_CONTAINS_IMAGE` 关系（file_id 已关联，检索够用）—— **延迟**
- [ ] `GET /resources/{id}/image`、`/{id}/thumbnail` 对外服务 + 前端图片引用块（Lightbox）—— 归 **1.5e**

### 1.5c 表格处理管道
- [x] 2026-07-28　**表格结构化（合并单元格）→ JSON**：`parsing/table_utils.py`（逻辑行 → 结构化 JSON + HTML[colspan] + 模板描述）；Word 经 python-docx `_tc` 标识识别**横向合并 col_span**；PDF 经 PyMuPDF `find_tables().extract()` 网格（col_span 暂全 1）。纵向合并 row_span（vMerge）/ PDF 精确 span 留后续 ✅
- [x] 2026-07-28　**表格描述 + HTML**：模板化描述（`N 行 × M 列表格；列：…`，同步免 LLM，LLM 增强留后续）；HTML 含 `<thead>/<tbody>` + colspan 供前端渲染 ✅
- [x] 2026-07-28　**大表格分片**：`doc_chunker` 重构为「文本块/表格块」有序切分；表格独立成 chunk（`chunk_content_type=table`，`chunk_id=tbl_{fh8}_p{page}_{order}`）；**>30 行**按 **20 行/片**分片（`table_fragment`，带表头上下文，`parent_table_chunk_id` 关联父表）✅
- [x] 2026-07-28　**`DocChunkSpec` + `_to_orm` 透传表格列**：table_data/table_html/table_description/table_total_rows/table_total_cols/is_table_fragment/table_fragment_index/parent_table_chunk_id（schema 已就绪，无新迁移）✅
- [x] 2026-07-28　**验收**：5 单测（docx 合并单元格结构化 / table_utils / 小表单 chunk / 大表分片 / `_to_orm` 透传）+ 真实 e2e（`tables.docx`：3×3 合并表→1 table chunk[headers/col_spans 正确] + 35 行大表→2 table_fragment[20+14, parent 关联]；`embedding_synced=true`、ES 命中）✅
- [ ] `doc_resources` 表写入（表格原件存 MinIO）—— 随 1.5b 图片管道引入 MinIO 时一并落地，**延迟**
- [ ] LLM 增强表格描述 / 纵向合并 row_span / PDF 表格精确 span（pdfplumber）—— **延迟**

### 1.5d 文档管理 API（8）+ 资源访问 API（3）+ 前端
- [x] 2026-07-28　**MinIO 客户端** `clients/minio_client.py`（懒加载 + 首次确保 bucket + put/get/remove 字节对象）；新增依赖 `minio`、`python-multipart`（UploadFile）✅
- [x] 2026-07-28　**`POST /v1/documents/upload`**：原件入 MinIO（`documents/{uuid}/{filename}`）→ `ingest_doc_bytes` 解析入库（复用 1.5a/c 管道，含 storage_path）；入库失败回滚删孤儿对象 ✅
- [x] 2026-07-28　**`GET /v1/documents`**（列表/分页/file_format 过滤，仅 storage_path 非空=已上传）、**`/{id}`**（详情）、**`/{id}/parse-progress`**、**`DELETE /{id}`**（§6.4 软删级联 + MinIO 删原件）✅
- [x] 2026-07-28　**`GET /v1/resources/{chunk_id}/table-data`**：返回 table/table_fragment chunk 的结构化表格（JSON+HTML+描述，复用 1.5c）✅
- [x] 2026-07-28　**`GET /v1/documents/{id}/tables`**：列文档的表格 chunk（chunk_id/行列/描述/是否分片，供前端表格预览）✅
- [x] 2026-07-28　**`services/document_service.py`**（同步编排，经 `asyncio.to_thread` 位置参数包装供 API）+ `schemas/document.py`；`ingest_doc_bytes`（内存字节入库，上传路径）✅
- [x] 2026-07-28　**前端文档管理页（1.5d-frontend）**：`api/documents.ts`（typed client，upload 走 multipart）+ `pages/DocumentsPage.tsx`（上传[AntD Upload customRequest] + 文档表[格式/解析状态/页/切片/大小] + 表格预览抽屉[列表格 chunk→渲染 table_html] + Popconfirm 删除 + 30s 轮询）+ App.tsx 路由 + global.css 表格样式；`pnpm typecheck`+`build` 通过 ✅
- [x] 2026-07-28　**验收**：5 后端单测 + HTTP API e2e（curl 全链路：multipart upload→list→/tables[3 表含分片]→table-data[HTML]→delete→404）+ 前端 typecheck/build ✅
- [ ] `POST /documents/upload/batch`、`GET /{id}/images` —— 随 1.5b 图片管道，**延迟**
- [ ] `GET /resources/{id}/image`、`/{id}/thumbnail` —— 随 1.5b 图片管道，**延迟**

### 1.5e 前端富媒体 + 接口改造
- [x] 2026-07-29　**图片引用块 + Lightbox**：`GET /v1/resources/{chunk_id}/image`、`/thumbnail`（查 doc_resources→MinIO 流式返回）；前端 `CitationCard` 图片引用渲染缩略图（AntD `Image`，点击灯箱看原图）✅
- [x] 2026-07-29　**表格引用块**：引用区分 `content_type`（chat_service 一次查询补 `chunk_content_type`+`image_description`，免改召回路径/ES mapping）；表格引用「查看」按钮→Modal 渲染 `table_html`（复用 `.coderag-html-table` 样式）✅
- [x] 2026-07-29　**验收**：前端 `pnpm typecheck`+`build` 通过；HTTP e2e——`/resources/{id}/thumbnail` 返回 PNG(4040B)、`/image`(5907B)；chat 查询 "ORDER 12345"→SSE citation `content_type="image"`、label="[图片] ORDER 12345"、score=1.0（图片经 OCR 文本被召回并正确标注）✅
- [ ] 全局搜索 / 会话详情 / 同步状态 支持 image/table —— **延迟**
- [ ] WebSocket `document.*` 事件 —— **延迟**

---

## Phase 2 — 精排模型

- [x] 2026-07-25　精排客户端 `clients/reranker_client.py`（OpenAI/SiliconFlow 兼容 `/rerank`；无 Key 复用 EMBEDDING_API_KEY；不可用优雅降级）✅ 实测 v2-m3 真实打分
- [x] 2026-07-25　检索管道接入精排 `retrieval/reranker.py`（Stage 2 粗排 + Stage 3 精排，每阶段独立 try/except 降级）✅
- [x] 2026-07-25　Stage 3 纯语义精排（v2-m3；图特征融合 α/β/γ 留待 Phase 5 graph_embedding）✅ 实测 relevance 0.99/0.66/…
- [ ] 模型服务：`bge-reranker-base`（粗排）—— **硅基流动无此模型**（HTTP 400 Model does not exist）；当前 COARSE 留空走单阶段精排。本地 `model_server` 提供独立粗排模型后可启用两阶段
- [x] 2026-07-25　前端检索详情漏斗（向量/词法/图遍历 → RRF → 粗排 → 精排，胶囊式展开）✅
- [ ] **验收**：A/B 验证精排使检索精度 +15~25%（需评测集，见横切·评测）

---

## Phase 3 — BM25 + 多路召回 + RRF（部分前置到 Phase 1）

- [x] `retrieval/bm25_search.py`：路径 B（ES BM25）✅ 已前置实现并接入管道
- [x] 路径 A+B（+D）并行召回 + 简单线性融合 ✅（Phase 1）
- [x] 2026-07-25　`retrieval/fusion.py`：RRF 正式融合（k=60；向量 1.0 / BM25·词法 0.8 / 图遍历 1.2），替换线性加分近似 ✅ 含 6 个单元测试
- [x] 2026-07-27　Stage 0 查询改写：`query_understanding.rewrite_query`（LLM 语义改写 + 补充关键词；未配置/失败优雅降级为原 query）✅
- [ ] **验收**：RRF 后召回率 +10~15%（需评测集）

---

## Phase 4 — 图遍历召回（部分前置到 Phase 1）

- [x] `retrieval/graph_traverse.py`：PG `call_graph` + `chunk_relations` BFS（上下游 N 跳）✅ 已前置实现并接入管道
- [x] Stage 0 实体/种子识别 → 图遍历召回入候选 ✅
- [x] RRF 加入图遍历（权重 1.2，随 Phase 3 RRF 一起落地，`fusion.DEFAULT_WEIGHTS["graph"]=1.2`）✅
- [x] 2026-07-29　**知识图谱模块 4 接口**：`schemas/graph.py` + `services/graph_service.py`（纯 helper：`aggregate_call_edges`/`group_module_edges`/`normalize_doc_edge` 单测覆盖；async 查询层用 `ANY(text[])` 同 graph_traverse 风格）+ `api/v1/graph.py` 4 端点（`/call-graph` 双向 BFS 两阶段「发现节点→取子图边计权」；`/code-doc-relations` 无向 BFS；`/module-dependency` 按 MODULE/PACKAGE/CLASS 聚合自环跳过；`/search` class/method/doc）；router 挂载。**无新迁移**。7 纯函数单测 + ruff + e2e（curl 4 接口全通）✅
- [x] 2026-07-29　**前端知识图谱页 + 右侧上下文面板**：`api/graph.ts`（typed client）+ `components/graph/CytoscapeGraph.tsx`（react-cytoscapejs，breadthfirst/circle/cose 三布局、按 type/stale 着色、hover 高亮相邻、tap 选节点、stale 标红）+ `pages/GraphPage.tsx`（图类型/布局/方向/深度/粒度切换、搜索选中心、节点详情抽屉、空/错态）+ `stores/app.ts` 加 `focused` + `components/ContextPanel.tsx`（复用 graph 接口渲染调用方/被调用方/关联文档）+ `CitationCard` 点击聚焦 + `Workbench` 替换占位 + `App.tsx` 路由。新增依赖 `cytoscape`/`react-cytoscapejs`/`@types/cytoscape`。typecheck+build 通过 ✅
- [ ] **验收**：接口+前端+e2e 已验证（✅）；调用链相关召回 +20%+ 需评测集（见横切·评测）

---

## Phase 5 — GNN 图向量　❌ 已弃用（2026-07-27）

> **决策**：移除图向量（graph_vec / 路径 C / GNN graph_embedding）。理由：用户裁剪——主链路仅保留
> 向量(unified 或 dual)+BM25+图遍历三路召回 + RRF + 精排。`graph_embeddings` 表已删 `graph_embedding`/
> `embedding_dim` 向量列，保留 pagerank/degree/community 等结构特征列供将来 GraphRAG；`fusion.DEFAULT_WEIGHTS`
> 已去 `graph_vec:0.9`。**图遍历（路径 D，PG call_graph BFS）保留**。GraphRAG 社区摘要（Phase 6）留作未来。
> 以下原任务不再开发，仅作存档：
- [~] ~~`graph/build_graph.py` / `gnn_model.py` / `train.py` / `graph_vector_search.py`（路径 C）~~ —— 已弃用

---

## Phase 6 — GraphRAG 社区摘要　❌ 已弃用（2026-07-29）

> **决策**：移除 GraphRAG 社区摘要功能，不再实现。理由：用户裁剪——不保留「社区检测 / 社区摘要 /
> 全局社区问答」这条链路。`graph_communities` / `node_community_mapping` 两表与 `graph_embeddings` 的
> `community_id_l0/l1/l2` 三列已删（迁移 `c8d3ea5f2b17`）；ORM `GraphCommunity`/`NodeCommunityMapping`
> 移除；前端「社区总览」侧栏项 + `/communities` 路由 + `NodeKind.community` 一并移除；Phase 7 中依赖社区
> 的检索/关联工具与「全局问答」Agent 同步删除（见下）。`graph_embeddings` 表（pagerank/degree/betweenness
> 通用结构特征）保留，供将来 LTR/重排复用。以下原任务不再开发，仅作存档：
- [~] ~~`graph/community.py`：Leiden 多层级社区检测（L0/L1/L2）~~ —— 已弃用
- [~] ~~LLM 批量社区摘要 → 向量化入 Milvus / 全局问答 Agent~~ —— 已弃用
- [~] ~~社区总览 4 接口 + 前端社区总览页~~ —— 已弃用

---

## Phase 7 — 多 Agent 体系

### 7.1 LangGraph 主图与子图
- [x] `agent/state.py`（State 字段，见技术栈 §8）
- [x] 主图地基（Milestone 1）：`query_analysis → retrieve → generate → post_process`，编译为 StateGraph，`RAG_ENGINE=langgraph` 开关后运行、默认 `legacy` 不破坏既有行为
- [x] 主图路由节点：`query_analysis` 加意图分类（with_structured_output，5 类 code/doc/graph/mixed/chitchat，无 key→规则兜底）+ `router` **意图→Agent 分发表**（`_INTENT_TO_AGENT_TYPE`+`_AGENT_TYPE_TO_NODE` 双字典；code→`code_understand`、doc→`doc_answer`、graph→`change_impact`，mixed/chitchat→`retrieve` 兜底；加场景 Agent = 两字典各加一行 + `graph.py` 加节点/边）。拓扑 `query_analysis→router→{code_understand | doc_answer | change_impact | retrieve→generate}→post_process`
- [ ] `aggregator` 聚合节点拆分（当前 retrieve 单节点包住 `pipeline.recall` 保行为同构，子图 fan-out 延后）
- [ ] 子图：code_agent / doc_agent / graph_agent（Send API 并行多路检索）
- [x] `memory/checkpointer.py`：MemorySaver（进程内）；`AsyncPostgresSaver`（断点续跑，需 `setup()` 建表）延后

> **2026-07-29 Milestone 1（地基）完成**：引入 `langgraph>=1.0` + `langchain-core>=0.3`（未引 `langchain-openai`，场景 Agent 工具调用时再引）；新增 `app/agent/{state,graph,streaming,nodes/*,memory/checkpointer}`。节点复用 `query_understanding` / `pipeline.recall` / `build_context` / `llm.stream_tokens`；节点内 `get_stream_writer()` 推 SSE 事件，`streaming.stream_graph` 适配器转成与 legacy `stream_chat` 同构的 `conversation→retrieval→citation→token→done` 序列并落库（会话/消息/RetrievalLog 复用 chat_service 抽出的持久化 helper）。`chat_service.stream_chat` 顶部按 `rag_engine` 分流。新增 `tests/test_agent_state.py` + `tests/test_agent_graph.py`（8 项，mock 检索/LLM，无需 infra）；全量 111 项绿、ruff 净。e2e（langgraph 引擎 + 真实 dual 检索/DeepSeek）事件序与 legacy 一致，会话/引用/RetrievalLog 落库可读回。**延后**（Milestone 2+）：router/aggregator 拆分、3 Send-API 检索子图、8 场景 Agent、20 `@tool`、`langchain-openai`、Agent/Task 表与 5 面板 API、前端面板、AsyncPostgresSaver。

> **2026-07-29 Milestone 2（Orchestrator + 代码理解 Agent，证明 Agent 闭环）完成**：引入 `langchain-openai>=0.3`（指向 DeepSeek）；新增 `app/agent/llm.py`（`ChatOpenAI` 工厂 + 意图分类 `with_structured_output` + `TokenSSEHandler` 回调）、`app/agent/nodes/router.py`（条件路由）、`app/agent/tools/{code_tools,formatting}.py`（5 个工具：search_code/search_symbol/get_call_chain/get_related_docs/read_code）、`app/agent/agents/code_understand.py`（**预置 `create_react_agent` 自动 Agent**，绑定 5 工具；包一层做 retrieval meta 前置 + token 回调注入 + 异常兜底 `_degrade`）。工具返回字符串观察（LLM 可见）+ 经 `get_stream_writer` 推 `agent_step`/`citation`；`code_understand` 用 `agent.astream(stream_mode="custom")` 把嵌套 custom 事件**桥接**到主图流；最终答案 token 经回调桥接。`query_analysis` 加意图分类；主图改条件路由拓扑。e2e（langgraph + dual + DeepSeek）：代码意图自动跑 6 步工具调用（search_symbol→search_code→read_code→get_call_chain→search_code→get_related_docs），事件序 `conversation→retrieval(mode:agent)→agent_step×6→citation×19→token×544→done`，引用按 chunk_id 落地、会话/消息持久化；doc 意图走 retrieve→generate 兜底（无 agent_step，行为同 Milestone 1）。新增 `tests/test_{agent_router,agent_tools,code_understand}.py`（22 项，mock 假 ChatModel+检索，无需 infra）；全量 133 项绿、ruff 净。**延后**（Milestone 3+）：doc/graph 等其余 7 场景 Agent、剩余约 15 工具、`aggregator`/3 Send-API 子图、Agent/Task 表与 5 面板 API、前端 `/agents` 页与 agent_step 可视化、AsyncPostgresSaver。

> **2026-07-29 Milestone 3（文档问答 Agent + 多 Agent 意图分发表）完成**：新增 `app/agent/agents/doc_answer.py`（**预置 `create_react_agent` 自动 Agent**，克隆 M2 模式，绑定 3 文档工具）+ `app/agent/tools/doc_tools.py`（`search_docs`=recall+`kind=="doc"` 过滤 / `read_doc`=doc_chunks 扩列 SELECT / `get_related_code`=get_code_doc_relations 反向 doc→代码佐证）+ `formatting.py` 3 个文档格式化器。**`router` 重构为意图→Agent 分发表**（`_INTENT_TO_AGENT_TYPE`+`_AGENT_TYPE_TO_NODE` 双字典，doc→`doc_answer`；加 Agent=两字典各加一行）；主图加 `doc_answer` 节点 + 条件映射 + `→post_process`。`code_understand.py`/`streaming.py`/`chat_service.py`/`llm.py` 零改动（事件契约形状驱动、已 doc 兼容）。e2e（langgraph + dual + DeepSeek）：文档意图自动跑 7 步工具调用（search_docs/read_doc，含 get_related_code 拉代码佐证），事件序 `conversation→retrieval(mode:agent,agent:DOC_ANSWER)→agent_step×7→citation×16→token×571→done`，单 retrieval 无降级、引用按 chunk_id 落库。新增 `tests/test_{doc_tools,doc_answer}.py`（13 项，mock 假 ChatModel+检索，无需 infra）+ 更新 `test_agent_router.py`（doc→doc_answer）；全量 146 项绿、ruff 净。**调优**：`agent_max_iterations` 默认 4→6（DeepSeek 偏多轮，4 触超限兜底；limit=4*2+3=11→6*2+3=15）。**延后**（Milestone 4+）：第 3+ 场景 Agent（变更影响/缺陷诊断等）、抽 `_base.run_scenario_agent` 共享骨架、Agent/Task 表 + 5 面板 API + 前端 `/agents` 页与 agent_step 可视化、AsyncPostgresSaver、`aggregator`/Send-API 子图。

> **2026-07-29 Milestone 4（变更影响 Agent + 抽 `_base.run_scenario_agent` 共享骨架）完成**：**抽 `app/agent/agents/_base.py`**——把 `code_understand`/`doc_answer` 逐字重复的节点骨架（`_safe_writer`/`_merge_callbacks`/`_emit_retrieval_meta`/`_degrade`/wrapper 控制流）收敛为 `run_scenario_agent(state,config,*,agent_name,tools,build_agent,degrade_label)`，三个 Explore agent 确认两 Agent 仅 5 处差异（agent_name/tools/prompt/build_agent 工厂/降级文案）；`code_understand.py`/`doc_answer.py` 改薄包装（prompt + 惰性单例 `get_X_agent()` + 转调 `_base`），行为零变更。**新增第 3 个场景 Agent 变更影响（CHANGE_IMPACT）**：`app/agent/agents/change_impact.py` 复用 4 个代码工具（search_symbol/search_code/read_code/get_call_chain）+ 新工具 `get_callers`（`code_tools.py`：CALLERS + depth + max_nodes=120，放 `get_call_chain` 硬编码 30 的上限；`format_impact_callers` 按 BFS 层归类、每层列前 8 + 折叠防 token 爆量）；意图路由 `graph→change_impact`（零改 `llm.py`，graph hints 已含「影响/上游/下游」）。e2e（langgraph + dual + DeepSeek，具名目标 `Foo.checkLocalTransaction`）：自动跑 6 步工具调用（search_symbol×2→search_code→read_code→**get_callers**→get_call_chain），事件序 `conversation→retrieval(mode:agent,agent:CHANGE_IMPACT)→agent_step×6→citation×8→token×340→done`，单 retrieval 无降级、Agent 自主用上 get_callers+get_call_chain 作答落库。测试 patch 目标随重构迁移（`configured`/`_enrich_content_types` 从 `cu`/`da` 改 patch `_base`）；新增 `tests/test_change_impact.py`（2）+ `get_callers`/`format_impact_callers` 单测 + 更新 `test_agent_router.py`（graph→change_impact）；全量 153 项绿、ruff 净。**延后**（Milestone 5+）：第 4+ 场景 Agent（缺陷诊断/代码审查/测试生成/文档维护/新人引导 5 个）、剩 11 工具、Agent/Task 表 + 5 面板 API + 前端 `/agents` 页与 agent_step 可视化、AsyncPostgresSaver、`aggregator`/Send-API 子图。

> **2026-07-30 Milestone 5（Agent 步骤可观测性：agent_step 持久化 + 回放 + 前端可视化）完成**：补齐「3 个场景 Agent 完全不可观测」的缺口——工具调用 `agent_step` 事件原本穿过两层流桥接到 SSE 后**在持久化边界被丢弃**（`streaming.py` 无 `agent_step` 累积分支、前端 `sse.ts` 无 case）。本轮：①**后端持久化**——`RetrievalLog` 加 `agent_steps` JSONB 列（迁移 `b7e2d09af3c1`，复用漏斗表同 `recall_results`/`fine_rank_results` 模式、零新表）；`streaming.py` 累积 `agent_step` 事件；`persist_retrieval_log` 透传写入。②**后端回放**——`GET /v1/chat/messages/{id}/retrieval` 增 `agent` 段（`{type, steps:[{tool,args,n}]}`），提取纯函数 `_build_agent_trace`（按 `agent_steps` 非空判定，覆盖降级前已跑若干步；`type` 取 `msg.agent_type`）。③**前端**——`sse.ts` 加 `agent_step` case + `onAgentStep` 槽；`useChat` 累积进 `ChatMessage.agentSteps`；`RetrievalDrawer` 加「Agent 推理轨迹」段（REST 回放，历史消息可见）；`ChatPage` 的 `RetrievalSummary` 有 `agentSteps` 时改渲染工具步骤 pill（顺带修 agent 消息展示全零漏斗的既有瑕疵，实时进度）。e2e（langgraph + DeepSeek，CHANGE_IMPACT）：`GET /retrieval` 返回 `agent.type=CHANGE_IMPACT`、4 步（search_symbol→search_code→get_callers→read_code，含 args+命中数）。新增 `tests/test_chat_helpers.py` 2 测（persist agent_steps fake-session / `_build_agent_trace`）+ `test_change_impact.py` agent_step shape 断言；全量 158 项绿、ruff 净、`alembic check` 无 diff、`pnpm typecheck` 过。**延后**（Milestone 6+）：Agent/Task 表 + 5 面板 API + 前端 `/agents` 页（跨会话聚合 today_calls/satisfaction）、第 4+ 场景 Agent、AsyncPostgresSaver、`aggregator`/Send-API 子图。**本轮不做完整面板**——聚焦 per-message 轨迹回放这一核心切片（完整面板为另一块 green-field 设计，留 M6）。

> **2026-07-30 Milestone 6（缺陷诊断 Agent + get_recent_changes 工具 + 前端下拉清理）完成**：第 4 个场景 Agent **缺陷诊断（BUG_DIAGNOSIS）**——前端下拉框早已承诺 `BUG_DIAGNOSIS` 但后端从未实现，本轮兑现。①**新 Agent** `app/agent/agents/bug_diagnosis.py`（克隆 change_impact，复用 `_base.run_scenario_agent` 骨架）绑 6 工具（search_symbol/search_code/read_code/get_call_chain/get_callers + 新 `get_recent_changes`），诊断 ReAct：定位→精读→调用上下文→回归排查→根因+修复建议。②**新工具** `get_recent_changes`（`code_tools.py`：查 `change_history` 表按 `chunk_id` 取最近 10 条 git 变更；`formatting.py` 加 `format_change_history`；**元数据工具只发 step 不发 citation**，空历史优雅降级为提示文 + `n=0`）。③**`bug` 意图**（`llm.py`：`IntentLabel`+bug / `_INTENT_SYS` / `_BUG_HINTS` 强信号 / `_rule_intent` 分支放 doc 前避免裸「异常」误判）；`router.py` 两表各加一行；`graph.py` 加节点+条件边+`→post_process`。④**前端下拉 bug 修复**：`ChatPage.tsx` `AGENTS` 的 `DOC_QA`→`DOC_ANSWER`（原值与后端不匹配致文档 Agent 经下拉框在 langgraph 模式不可达）、删 Phase 6 已弃用的 `GLOBAL_QA`，保留 4 个真实 Agent。新增 `tests/test_bug_diagnosis.py`（2：循环事件序 + 降级）+ `test_agent_router.py`（3：bug→bug_diagnosis 路由 + `_rule_intent` 强信号 + 不误判 doc）+ `test_agent_tools.py`（5：`get_recent_changes` 格式化/逻辑/@tool step-only/空历史）；全量 168 项绿、ruff 净、`pnpm typecheck` 过。**边界**：`change_history` 仅增量同步写入，样本库（全量入库）为空——e2e 验证工具优雅降级（返回提示文），核心验收聚焦诊断主流程（定位→精读→调用图→根因）+ 路由 + 可观测性；待增量同步跑过后回归排查角度自然生效。**延后**（Milestone 7+）：第 5+ 场景 Agent（代码审查/测试生成/文档维护/新人引导 4 个）、剩 10 工具、`aggregator`/Send-API 子图、Agent/Task 表 + 5 面板 API + 前端 `/agents` 页、AsyncPostgresSaver。

> **2026-08-04 Milestone 7（Agent 面板 `/agents` + 聚合统计 API）完成**：兑现前端早已占位的「Agent 面板」路由/导航（`App.tsx` 占位 → `<AgentsPage/>`；`Workbench.tsx` 导航项 `/agents` 早已存在）。**关键决策：零新表、零迁移、零 Agent 运行时改动**——经源码核实 5 指标全部可从既有 `retrieval_logs` 按需聚合（`mode:'agent'`=Agent 成功跑完的可靠信号，仅 `_base._emit_retrieval_meta` 写、`_degrade` 覆盖丢失；`agent_steps` JSONB=工具轨迹；`user_feedback`=满意度；降级=`agent_steps` 非空且 `mode` 缺失）。①**后端**：`app/services/agent_stats_service.py`（async，本仓库首个 GROUP BY/AVG/case/jsonb_typeof 用例）+ `app/schemas/agent.py` + `app/api/v1/agents.py`（`GET /v1/agents/stats` = KPI + 各 Agent 明细、`GET /v1/agents/runs` = 分页流水，仿 `sync.py`；窗口 today/7d/all，`pattern` 校验）。降级 run 丢 `recall_results->>'agent'` 标签 → `LEFT JOIN chat_messages` 以 `coalesce(meta->>'agent', chat_messages.agent_type)` 恢复归属。②**前端**：`pages/AgentsPage.tsx`（仿 SyncPage：4 KPI tile + 各 Agent 明细表 + 最近运行表 + 20s 轮询，纯 Statistic/Table/Tag 无图表库）+ `api/agents.ts`。③**修一个真 bug**：`jsonb_array_length(agent_steps)` 对个别遗留**标量**行抛 `cannot get array length of a scalar`（`/runs` 选全部 engaged 行触发；`/stats` 因外层 CASE 守护未触发）→ 改 `case((jsonb_typeof(agent_steps)='array', jsonb_array_length(...)), else_=None)` 守护，标量/对象/NULL → 0/NULL。新增 `tests/test_agent_stats.py`（11：纯 helper `_ratio`/`_since` + 假 session 编排 KPI/per_agent/runs 组装 + 路由注册冒烟）；全量 179 项绿、ruff 净、`pnpm typecheck` 过。**e2e（langgraph，真实历史数据）**：`/stats?window=all` → 6 成功 + 4 降级 = 10 介入、`degradation_rate=0.4`、`avg_steps=4.0`、满意度 1/1、per_agent 含 4 Agent（降级 run 经 chat_messages 回退有标签）；`/runs` → 10 条、降级 run `degraded:true`+标签恢复、标量行 `steps:0` 不崩；today=0（空态优雅）、7d 含数据、invalid `month`=422。**降级率口径**：分子=「发过若干步后兜底」（agent_steps 非空且 mode 缺失），不含「无 key 未进 Agent」的预兜底（配置问题非 Agent 质量）。**延后**（Milestone 8+）：第 5+ 场景 Agent（4 个）、剩 10 工具、`aggregator`/Send-API 子图、AsyncPostgresSaver、Agent/Task 物化汇总表（当前规模按需聚合足够）。

> **2026-08-04 Milestone 8（AsyncPostgresSaver 持久化——断点续跑地基）完成**：兑现 `checkpointer.py` 里自 M1 起标注的 `NotImplementedError` 桩——`LANGGRAPH_CHECKPOINT=postgres` 时用 `AsyncPostgresSaver`（`langgraph-checkpoint-postgres==3.1.1`）替代 `MemorySaver`，主图 thread 状态（`thread_id=conversation_id` 已就绪于 `streaming.py:40`）持久化到 PG 的 `checkpoints`/`checkpoint_writes`/`checkpoint_blobs`/`checkpoint_migrations` 四表（`setup()` 自建、幂等），**跨进程重启存活**，是 resume/中断/HITL/跨轮记忆（M9+）的共同地基。①**config** 加 `postgres_dsn`（裸 `postgresql://`——langgraph 要原生 psycopg DSN，不能复用 `database_url_sync` 的 `+psycopg`）。②**checkpointer.py** 重写：`get_checkpointer()` memory 分支不变，postgres 分支返回 lifespan 注入的 saver（未初始化→RuntimeError 不静默崩）；新增 `init_checkpointer()`/`close_checkpointer()`（`AsyncPostgresSaver.from_conn_string` 上下文管理器经手动 `__aenter__`/`__aexit__` 跨 lifespan 存活 + `setup()` 建表；非 langgraph/memory no-op）。用 `from_conn_string` 而非裸 `AsyncConnectionPool`——前者配 autocommit 使 `setup()` 的 `CREATE INDEX CONCURRENTLY`（须事务外）可执行。③**main lifespan** 接 init/close。④**alembic/env.py** 加 `include_object` 排除 langgraph 四表防 autogenerate 误删。⑤**踩两个 Windows 硬坑（已修，写入 CLAUDE.md 平台坑段）**：(a) psycopg async 需 `SelectorEventLoop`，默认 Proactor 不兼容；(b) **uvicorn 0.51 在 win32 硬编码 ProactorEventLoop**——`Config.get_loop_factory` 直接返回 `asyncio.ProactorEventLoop`、**绕过事件循环策略**，故 `set_event_loop_policy` 无效；解法：仅在启用 postgres 检查点时覆盖 `get_loop_factory` 为 Selector（import app.main 早于 server.run 取 factory，故生效）。新增 `tests/test_checkpointer.py`（9：postgres_dsn 格式 / get_checkpointer 三分支 / init no-op 双路径 / init-close 生命周期 fake cm）；全量 **188** 项绿、ruff 净、`alembic autogenerate` 干跑无 diff。**e2e（langgraph+postgres+真实 PG）**：CODE_UNDERSTAND 跑通（mode:agent + agent_step + citation），turn1 持久化 18 checkpoints；同 thread turn2 加载后续跑；**重启 backend 后 turn3 在全新进程加载持久化 thread 状态正常作答 110 token**、checkpoints 计数 18→34——跨重启断点续跑核心证明。默认 legacy/memory 路径零变更（`:8000` 不受影响）。**延后**（Milestone 9+）：`astream(None,config)` 续跑 API、human-in-the-loop 中断、跨轮 `messages` 累积接线（场景 Agent 当前每轮重建 messages，跨轮收益主要体现为主图 state 存活 + 后续地基）、checkpoint 老化清理、Agent/Task 物化汇总表。

> ✅ 2026-08-04 **Phase 7 Milestone 9（跨轮对话记忆——让 LLM 携带会话历史）完成**：M8 落地 AsyncPostgresSaver 后实测暴露的真实缺口——每轮图都以全新 `state={"query":...}`（`streaming.py`）启动，`generate` 节点 / 场景 Agent / `build_messages` **只看当前 query**，langgraph 与 legacy 两路 LLM **跨轮完全失忆**（侧边栏能显示历史、模型却看不见，「它/刚才那个」指代必然失败）。本轮从 `chat_messages`（真相源）载入先前轮次注入消息：①**`chat_service`** 加 `load_conversation_history(session, cid, *, exclude_message_id, limit)`（`DESC LIMIT N` 取最近 N 条→反转为升序时序、每条截断 `_HISTORY_MSG_MAX_CHARS=1200` + `…[已截断]` 标记、排除当前轮）+ `build_messages(..., history=None)`（`if history:` 守卫，None/[] 与今天逐字一致→188 测零回归，插在 system 与当前 user 间）；`add_user_message` 改返回 message_id 供排除当前轮。②**`config`** 加 `conversation_history_turns`（默认 6；0=禁用→逐字同今天）。③**langgraph**：`stream_graph` 载入历史注入 `state["history"]`（新只读字段、无 reducer——每轮新值覆盖 checkpoint 快照），`generate` 节点 + `_base.run_scenario_agent`（Agent 消息种子 `[*history, 当前 query]`）+ `_degrade` 消费；**legacy `stream_chat`** 同样载入传给 `build_messages`。**历史来源 = chat_messages 而非 checkpointer**（两路共同真相源 / 不依赖 checkpoint 模式 / Agent 不把消息写回 checkpoint state）；M8 持久化与 DB 历史**互补**不重叠。新增 `tests/test_conversation_history.py`（10：loader 短路/查询构造/升序/截断/形状 + build_messages 回归 + generate 接线 + `_base` 种子）；全量 **198** 测绿 + ruff 净。**e2e（三重证明）**：① legacy 两轮——turn1 列 Foo 方法、turn2「它的主要方法有哪些？」（"它"无 Foo 关键词、不可检索）正确解析为 Foo 并列出同方法；② langgraph+postgres 两轮（mode:agent）——Agent 经种子历史解析"它"=Foo 作答（**M8+M9 共存**：postgres 检查点 + 跨轮记忆同时生效）；③ **kill-switch**——`CONVERSATION_HISTORY_TURNS=0` 时 turn2「我上一个问题是什么？」答「这是第一条消息、无提问记录」（无记忆、逐字同 M9 前）。前端零改动、SSE 契约不变。**延后**（Milestone 10+）：`astream(None,config)` 续跑 API、HITL 中断、checkpoint 老化清理、Agent/Task 物化汇总表、第 5+ 场景 Agent、aggregator/Send-API 子图。详见 [开发清单.md](./开发清单.md) §7.1。

> ✅ 2026-08-04 **Phase 7 Milestone 10（HITL 人在回路中断）完成**：兑现 M8 持久化地基的旗舰用法——Agent 在执行写动作前 `interrupt()` 暂停，前端弹审批框，人工批准/拒绝后 `Command(resume=...)` 续跑，interrupt 状态经 M8 checkpoint **跨进程重启可恢复**。**关键技术裁决**：`interrupt()` 放**主图节点**（非嵌套 create_react_agent 内——研究证实子图内 interrupt 的 resume 会重启而非续跑 [LG #4796/#5863/#6792]，且 `_base.py:144` 的 except 会吞 `GraphInterrupt`）；主图单一 checkpointer + `Command(resume=...)` 干净续跑。①**新主图分支**（显式 `agent_type=DOC_MAINTAIN` 触发，opt-in 隔离、4 既有 Agent 零影响）：`propose`（召回+选锚点+非流式起草提案）→ `confirm`（`interrupt(proposal)`，极简——resume 重跑本节点）→ 条件边 → `apply`（写 `chunk_relations.is_stale=True`，流式 ✅）/ `reject`（❌，不改库）→ `post_process`。被闸门写动作=标记锚点过时（真实、可逆、复用既有列、无新表）。②**中断检测在适配器层**：`stream_graph` 的 astream 循环后 `aget_state`，`tasks[*].interrupts` 非空→落「interrupted」消息、发 `interrupt` 事件、**不发 done**；新 `resume_graph` 用 `Command(resume=decision)` 同 thread 续跑→流 token→finalize 消息为 completed→done。③**新 `POST /v1/chat/resume`**（langgraph 专属，legacy→501）+ `ChatMessage.status` 列（completed|interrupted，迁移 `f4a2c7e91b38`，`server_default` 保既有行安全）+ `ResumeRequest` schema。④**前端**：`sse.ts` 加 `interrupt` 事件分发 + `streamResume`；`useChat` 加 `interrupt` 态 + `resume()`；`ChatPage` 加「文档维护 Agent」+ 审批 Modal（批准/拒绝+备注，`maskClosable/keyboard=false` 强制显式决策）。新增 `tests/test_hitl.py`（13 项，纯 mock：`_pick_stale_candidate`/`_apply_stale_mark`/propose/apply/reject 节点/条件路由/中断检测/续跑）；全量 **211** 测绿 + ruff 净 + `alembic check` 无 diff + 前端 typecheck/build 通过。**e2e（四重证明，langgraph+postgres+DeepSeek）**：① interrupt——`conversation→retrieval→citation×8→interrupt`（真实 LLM 起草提案，**无 done**）+「interrupted」消息落库；② approve——`token(✅)→done`、`chunk_relations.is_stale` 翻转、消息 completed；③ reject——`token(❌)→done`、**无写**；④ **跨重启**——中断后**杀进程+重启 backend**，`/resume` 从 PG checkpoint 续跑、`is_stale` 翻转（**M8 持久化兑现**，4→3→2 验证链）。**延后**（Milestone 11+）：`astream(None,config)` 通用续跑 API、HITL 图级超时、checkpoint 老化清理、Agent/Task 物化汇总表、第 5+ 场景 Agent（完整文档维护 ReAct）、aggregator/Send-API 子图。

> ✅ 2026-08-04 **Phase 7 Milestone 11（代码审查 Agent）完成**：交付第 5 个完整场景 Agent **代码审查（CODE_REVIEW）**——主动评估代码质量/发现潜在问题/给改进建议（区别于 code=解释逻辑 / bug=诊断已报告失败 / graph=影响范围）。①**新 Agent** `app/agent/agents/code_review.py`（克隆 bug_diagnosis，复用 `_base.run_scenario_agent` 骨架）绑 6 工具（search_symbol/search_code/read_code/**get_code_metrics**/get_call_chain/get_recent_changes）。②**新工具 `get_code_metrics`**（`code_tools.py`：LOC + token_count + call_graph 条件 COUNT fan-in/fan-out，纯整数列无脆弱解析；`formatting.py` 加 `format_code_metrics` 带阈值提示；**元数据工具只发 agent_step 不发 citation**，区别于「换 prompt 的 bug_diagnosis」）。③**新 `review` 意图**（`llm.py`：IntentLabel/IntentSchema/_INTENT_SYS/_REVIEW_HINTS；`_rule_intent` 优先级 graph>bug>**review**>doc>code）；`router.py` 两表各加一行；`graph.py` 加节点+条件边。新增 `tests/test_code_review.py`（2：循环事件序 + 降级）+ `test_agent_router.py`（3：review 路由 + `_rule_intent` review/不误吞）+ `test_agent_tools.py`（5：`_get_metrics` 逻辑/缺失 + `@tool` 仅 step + `format_code_metrics`）；全量 **221** 测绿 + ruff 净 + `alembic check` 无 diff（零迁移）+ 前端 typecheck/build 过。前端 `ChatPage.tsx` `AGENTS` 加「代码审查 Agent」（`useChat` 零改动，对 agent_type 泛化）。**e2e**（langgraph+DeepSeek，`Account.withdraw`）：LLM 自主跑 11 步工具调用（search_symbol→search_code→read_code→**get_code_metrics**(LOC=18/fan-in=0/fan-out=1)→get_recent_changes→get_call_chain…），事件序 `conversation→retrieval(mode:agent,agent:CODE_REVIEW)→agent_step×11→citation×37→token×1690→done`，回答按严重度分级（🔴 负数金额校验缺失→资金漏洞）+ 改进建议；`GET /retrieval` 回放 `agent.type=CODE_REVIEW` + 11 步轨迹；**意图路由**：中文「帮我审查…改进建议」/「需要重构吗」→ classify=review → code_review。**延后**（Milestone 12+）：第 6+ 场景 Agent（测试生成/新人引导）、剩 9 工具、aggregator/Send-API 子图、运营加固（通用续跑 API/HITL 超时/checkpoint 老化）、Agent/Task 物化汇总表。详见 §7.1-7.3。

> ✅ 2026-08-04 **Phase 7 Milestone 12（测试生成 Agent）完成**：交付第 6 个完整场景 Agent **测试生成（TEST_GENERATION）**——为指定 Java 方法/类生成 JUnit 单元测试（覆盖正常/边界/异常），区别于既有 Agent（code=解释 / bug=诊断失败 / review=评估质量 / graph=影响）。①**新 Agent** `app/agent/agents/test_generation.py`（克隆 bug_diagnosis，复用 `_base.run_scenario_agent`，**`_base.py` 零改动**）绑 5 工具（search_symbol/search_code/read_code/get_call_chain/**get_existing_tests**）。②**新工具 `get_existing_tests`**（`code_tools.py`：解析 center→class_name（`class:` 前缀/chunk_id 查询/原样）→ `class_name ILIKE '{Class}%Test'` 找现有测试类 LIMIT 8，前缀锚定可走 `idx_code_chunks_class` 索引；**内容工具发 citation**（区别于 get_recent_changes/get_code_metrics 的仅 step），空→`format_existing_tests` 提示按 JUnit5+Mockito 通用约定生成）。③**新 `test` 意图**（`llm.py`：IntentLabel/IntentSchema/_INTENT_SYS；`_TEST_HINTS` **不收裸 test/mock**——否则 `any(h in q)` 子串匹配会把 "latest changes"(la**test**) 误判；`_rule_intent` 优先级 graph>bug>review>**test**>doc>code）；`router.py` 两表各加一行；`graph.py` 加节点+条件边。新增 `tests/test_test_generation.py`（2：循环事件序 + 降级）+ `test_agent_router.py`（3：test 路由 + `_rule_intent` test + 去伪命中反例）+ `test_agent_tools.py`（6：`_get_existing_tests` chunk_id 解析/`class:` 前缀/缺失/`@tool` 发 citation + `format_existing_tests` populated/empty）；全量 **232** 测绿（221→232，+11）+ ruff 净 + `alembic check` 无 diff（零迁移）+ 前端 typecheck/build 过。**踩一个 pytest 收集碰撞坑（已修）**：agent 模块文件 `test_generation.py` 与节点函数 `test_generation` 均以 `test_` 开头 → pytest 误当测试收集（`fixture 'state' not found`）；两处互补修复：①测试文件用模块属性 `tg.test_generation` 而非直接 `from ... import test_generation`（避免裸名进测试命名空间）；②`pyproject.toml` 加 `testpaths=["tests"]`（无参 `pytest` 不再扫 `app/`，对未来 app/ 下任何 `test_*.py` 免疫）。前端 `ChatPage.tsx` `AGENTS` 加「测试生成 Agent」。**e2e**（langgraph+DeepSeek，`Foo.checkLocalTransaction`）：LLM 自主跑 **14 步**工具调用（search_symbol×2→search_code×2→read_code→get_call_chain→read_code→**get_existing_tests**(空态)→search_code×4→get_call_chain→search_code），事件序 `conversation→retrieval(mode:agent,agent:TEST_GENERATION)→agent_step×14→citation×64→token×2022→done`，回答含可编译 JUnit（`@Test`/`assert`/Mockito `spy`/`verify`/`import org.junit`），正确解析 `checkLocalTransaction→resolveHalfMsg→sendCheckMessage+rollback(id=null)` 逻辑；`GET /retrieval` 回放 `agent.type=TEST_GENERATION` + 14 步（含 get_existing_tests）；**意图路由**：「write junit unit tests…」→ classify=test → TEST_GENERATION；「latest changes」→ CODE_UNDERSTAND（**去伪命中**，未误判 test）。**延后**（Milestone 13+）：第 7+ 场景 Agent（文档维护完整 ReAct/新人引导）、剩 8 工具、aggregator/Send-API 子图、运营加固（通用续跑 API/HITL 超时/checkpoint 老化）、Agent/Task 物化汇总表。详见 §7.1-7.3。

> ✅ 2026-08-05 **Phase 7 Milestone 13（文档维护完整 ReAct Agent）完成**：把 M10 的 DOC_MAINTAIN HITL 桩（硬编码 propose：单次 recall + 取 confidence 最高锚点 + 一句套话起草）**升级为真正的 ReAct Agent**——LLM 自主排查「哪些文档锚点与代码脱节」。①**重写 propose 节点**为 ReAct：LLM 用工具定位代码 → `detect_stale_docs` 找 DOC↔CODE 锚点 + staleness 证据（交叉 change_history）→ read_code/read_doc 精读两侧 → get_recent_changes 看代码近期改动 → `submit_proposal` 提交**结构化、可多锚点、有证据**的提案。②**关键设计**：结构化提案经内部协议事件 `_proposal_captured`（submit_proposal 推、propose 在 astream 循环捕获、**不桥接到 SSE**）流回主图——复用 agent_step/citation 同一 custom 通道、零新机制；submit_proposal 先在 DB 校验 relation_id（DOC_TO_CODE/CODE_TO_DOC 且未过时）。`interrupt()` 仍在主图 confirm 节点（M10 约束不变）。③**新工具**：`tools/maintain_tools.py` 的 `detect_stale_docs`（内容工具，发 citation，§7.2 变更历史类，工具 12→**13/20**）+ `submit_proposal`（ReAct 终结控制工具，args_schema 校验 relation_ids）。④**泛化 apply** 循环标记多锚点；状态 `stale_anchor`→`stale_anchors`(list)；无 LLM key → 降级到 M10 硬编码 `_propose_fallback`（HITL 仍可演示）；ReAct 异常 → 降级 token + 空 anchors，**永不中断请求/中断前不漏 token**。新增 `tests/test_maintain_tools.py`（11）+ `test_hitl.py` ReAct 路径 4 测；全量 **247** 测绿（232→247，+15）+ ruff 净 + alembic 零迁移 + 前端 typecheck 过。**opt-in 不加 intent**（写动作 Agent 保持显式触发，区别于 6 个只读 Agent）。前端零改动（下拉 + 审批 Modal 沿用 M10；agent_step 轨迹经 M5 在抽屉自动可见）。**e2e**（langgraph+postgres+DeepSeek）：3 次 ReAct 运行（6/10/7 步）证明 Agent 自主用新工具 + 证据驱动判断——无真实过时时**正确拒绝**（代码 DELETED→RESTORED 净效果无变化，文档仍准确），发现真实 doc/code 不一致（Profile.formatName 文档漏写「空名返回 'unknown' 兜底」）时才提案；`submit_proposal → interrupt(无 done) → /resume approve → ✅ token+done、is_stale 翻转 + stale_reason 写入`；`GET /retrieval` 回放 `agent.type=DOC_MAINTAIN` + 7 步轨迹（**M10 propose 无轨迹，M13 新增可观测性**）。**延后**：generate_doc_update（实际重写文档入 MinIO）/create_doc_pr、PR webhook 触发、定时腐化检测、剩 7 工具、新人引导 Agent、aggregator/Send-API 子图、运营加固（通用续跑/HITL 超时/checkpoint 老化）。详见 §7.1-7.3。

> ✅ 2026-08-05 **Phase 7 Milestone 14（运营加固·持久化地基）完成**：补 M8/M10/M13 堆叠的有状态基建三缺口。①**checkpoint 老化清理**（`app/agent/memory/checkpoint_cleanup.py`：`delete_thread_checkpoints`/`cleanup_old_checkpoints` **整 thread 过期**——最新 checkpoint ts 早于保留期才整删三表、活跃线程全保留、无「删一半」resume 歧义；用独立 `psycopg.AsyncConnection`（隔离 saver 的 `asyncio.Lock`）；checkpoint 表无时间戳列→用 `checkpoints.checkpoint->>'ts'` JSONB 键；保留期 30d ≫ 超时 24h 故不误删待审批活 thread）。②**HITL 图级超时**（`app/services/maintenance_service.py` `expire_stale_interrupts`：超时未 resume 的 interrupted→`status='expired'` + 超时说明；`main.py` lifespan 维护循环 `asyncio.create_task` 仿 `ingest_resync`，每轮过期中断 + 清超龄 thread checkpoint；`config` 加 `maintenance_enabled`/`hitl_interrupt_timeout_hours`/`checkpoint_retention_days`/`maintenance_interval_seconds`，dev 默认关）。③**线程状态查询 + 通用续跑**（`GET /v1/chat/conversations/{id}/state` 暴露 `has_pending_interrupt`/proposal/`age_hours`；`POST /v1/chat/continue` 用 `astream(None)` 推进非 interrupt 中途暂停线程[断流恢复]，遇 interrupt 如实上报不推进、无 pending 节点 noop done；`MessageItem.status` 暴露 + `load_conversation_history` 过滤 interrupted/expired 占位）。**修一个真 bug**：continue 对无 checkpoint 线程调 `astream(None)` 抛 `EmptyInputError` → 加 `snap.next` 闸门（无 pending 节点直接 noop）。`/resume` 加 **409**（非 interrupted 态拒绝，过期/已完成干净失败）。**零迁移/零新表/零新依赖/6 Agent + DOC_MAINTAIN 运行时零改动**。新增 `test_checkpoint_cleanup.py`(5)+`test_maintenance.py`(5)+`test_hitl.py` continue/state 5 测；全量 247→**262** 测绿 + ruff 净 + alembic 零迁移。**e2e**（langgraph+postgres+DeepSeek）：真实 DOC_MAINTAIN interrupt → `GET /state`(has_pending=true/age)/`POST /continue`(上报 interrupt 不推进)；回退消息 2h → 维护循环(超时 1h) → `status=expired` + 该 thread **11 checkpoint 行全删**；`GET /state`(has_pending=false)/`POST /resume`(**409**)/`POST /continue`(noop done)；年龄清理插 35d 旧 thread → **三表删/近期 thread 保留**（`{'threads':1,'rows':3}`）。**延后**：实际重写文档/PR、PR webhook、定时腐化检测（detect_stale_docs 扫全量）、剩 7 工具、新人引导 Agent、aggregator/Send-API 子图、Agent/Task 物化表。详见 §7.1/§7.4。

> ✅ 2026-08-05 **Phase 7 Milestone 15（DOC_MAINTAIN 写动作闭环——generate_doc_update + create_doc_pr）完成**：兑现 M10→M14 整条 HITL 弧线的实际产出——审批通过后 `apply` 不再只翻 `is_stale`，而是 **LLM 据当前代码重写过时文档段落 → 工件写回 MinIO → 装配结构化 PR 提案落库**。①**新服务** `app/services/doc_maintenance_service.py`：`generate_doc_update`（取 doc+code chunk → `llm.chat` 重写 → `minio_client.put_bytes` 写自包含 markdown 工件；无 LLM/chunk 缺失/LLM 或 MinIO 失败 → `rewritten_ok=False` 降级，永不抛）+ `create_doc_pr`（纯 helper `_branch_name`/`_commit_message`/`_unified_diff`(difflib) + 插 `DocUpdateProposal` 行；有重写→`PENDING_PUSH`、无→`PENDING_MANUAL`、落库失败→`FAILED`+rollback）。②**扩 `apply_stale`**（`doc_maintain.py`）：保留 is_stale 标记循环，其后**按 doc_chunk_id 去重**（`_split_anchor` 按 relation_type 派生 doc/code 侧；多锚点同段落只重写一次、`relation_ids` 聚合）→ generate_doc_update → create_doc_pr → 发 ✅ + 📝 摘要 token。③**新表 + 迁移** `doc_update_proposals`（proposal_id/conversation_id/file_id/doc_chunk_id/heading_path/relation_ids/original_text/rewritten_text/artifact_key/branch_name/commit_message/status/pr_url/error_message/source_commit；FK→doc_files；env `_include_object` 不排除故 autogenerate 收入）。④config 加 `doc_update_artifact_prefix`。**范围裁决**：Fork X（先布尔审批后生成，Fork Y 预览 diff 延后）；create_doc_pr **仅产出 PR 载荷、不执行真实 git**（扩展点）；新 SSE 事件/前端零改动（结果走 token，工件+表行是持久记录）。新增 `tests/test_doc_maintenance_service.py`(9) + 扩 `test_hitl.py` apply 3 测（含去重聚合）；全量 262→**272** 测绿 + ruff 净 + alembic 一表迁移 + `alembic check` 无 diff。**e2e（langgraph+postgres+DeepSeek，两段证明）**：①服务级——对真实 PG/MinIO/DeepSeek 直跑 generate_doc_update+create_doc_pr（relation 6 DOC_TO_CODE → 真实重写 + MinIO 工件 305B 可读回 + PENDING_PUSH 行）；②全图——DOC_MAINTAIN 触发→ReAct 提案 2 锚点（relation 7,8 Profile.formatName 文档漏写「空名返回 unknown」）→`interrupt`→`/resume` approve→apply 发 `✅ 已将 2 个锚点标记为过时` + `📝 文档更新提案 #2（分支/状态 PENDING_PUSH/工件）`、`is_stale` 翻转、**1 行提案聚合 relation_ids=[8,7]**（去重证明）、MinIO 工件可读、`GET /retrieval` 回放。收尾还原样本 KG（0 stale/0 提案/删 9 测试会话+checkpoint 三表 747 行）。**延后**：Fork Y（审批前预览重写 diff+前端）、替换 `doc_maintenance_stub.close_open_doc_pr_for`（回滚关 PR 联动）、远程 PR(`pr_url`)/真实 git 提交、整文档级重写、定时腐化检测、剩 5 工具、新人引导 Agent、aggregator/Send-API 子图、Agent/Task 物化表。详见 §7.1-7.4。

> ✅ 2026-08-05 **Phase 7 Milestone 16（主动腐化巡检——proactive staleness sweep）完成**：让 DOC_MAINTAIN 从「被动应答」变「主动巡检」——定时后台任务枚举全库非过时 DOC↔CODE 关系，据 `change_history` 证据用**全库首条自动过时规则**判定候选并自动标记 `is_stale=True`，经 `GET /v1/staleness/report` 暴露。重写仍走 M15 HITL 闸门；M16 只负责**检测+标记+可观测**，不重写。①**判定规则**：非过时 DOC↔CODE 关系，其代码侧 `change_history` 存在 `change_type IN ('MODIFIED','DELETED')` 且 `git_commit_time > relation.updated_at` ⇒ 过时候选（ADDED/RESTORED/ROLLBACK 不计；DELETED 路径 soft_delete 已实时处理，巡检补 MODIFIED）。②**新服务** `app/services/staleness_sweep_service.py`（raw text()，镜像 maintenance_service）：`run_staleness_sweep`（枚举 `_RELATIONS_SQL`→按方向约定派生 code chunk_id（`DOC_TO_CODE`=target / `CODE_TO_DOC`=source）→一次 `_LATEST_CHANGE_SQL`(`DISTINCT ON chunk_id`) 避免 N+1→判定→`_MARK_SQL` 标 `is_stale=true, stale_reason="SWEEP:{type}@{hash8} {msg}"`，仿 soft_delete 的 `DELETED:{commit}`；幂等仅扫 `is_stale=false`；永不抛→error dict）+ `build_staleness_report`（`count + FILTER` 按 `stale_reason` 前缀分解 SWEEP/DELETED/other + recent）。③**周期任务**（`main.py`）：`_run_staleness_sweep_once`+`_staleness_sweep_loop` 镜像 M14 维护循环（`asyncio.create_task`+`finally` cancel），**仅 gate `staleness_sweep_enabled`、不要求 langgraph**（巡检纯 PG，legacy 模式亦可用，不同于 M14 维护循环）。④config 加 `staleness_sweep_enabled`/`staleness_sweep_interval_seconds`/`staleness_sweep_batch_size`；新 schema `staleness.py`(StalenessFinding/StalenessReport) + 端点 `staleness.py` `GET /v1/staleness/report`。**零迁移/零新表/零新依赖/6 Agent 运行时零改动**（HITL 闸门历来只管「重写」不管「标记」，故自动标记与 soft_delete 一致；§7.2 @tool 计仍 13/20）。新增 `tests/test_staleness_sweep_service.py`(14)；全量 272→**286** 测绿 + ruff 净 + `alembic check` 无 diff（零迁移）。**e2e（真 PG，两段证明）**：①服务级——快照→种 `change_history`(MODIFIED, 晚于关系)→直跑 sweep→目标 code chunk 的**两个**关系（CODE_TO_DOC rel5 + DOC_TO_CODE rel6，**证明方向约定两端都对**）`is_stale` 翻转、`stale_reason='SWEEP:MODIFIED@m16e2ebe …'`、报告 `by_source.sweep=2`+recent 含两关系、再跑 marked=0（幂等）→快照还原；②循环 smoke——`staleness_sweep_enabled=true`+间隔 4s 起 backend(:8003)：启动日志 `后台巡检循环已启用：间隔 4s` + 4s 后 tick `[staleness-sweep] 巡检完成 {scanned:4,marked:2}` + `GET /staleness/report` 返回 `sweep:2`。收尾还原样本 KG（0 stale/0 种子）。**延后**：SWEEP 标记关系的批量重写（M17 候选）、评审队列/前端腐化面板、Fork Y（审批前预览重写 diff+前端）、替换 `doc_maintenance_stub.close_open_doc_pr_for`（回滚关 PR 联动）、远程 PR(`pr_url`)/真实 git 提交、整文档级重写、剩 5 工具、新人引导 Agent、aggregator/Send-API 子图、Agent/Task 物化表。详见 §7.1-7.4。

> ✅ 2026-08-05 **Phase 7 Milestone 17（SWEEP 批量重写——batch rewrite → 审批队列）完成**：桥接 M16 检测→M15 重写——M16 标记的 SWEEP 过时关系对反应式 HITL 路径**不可见**（`detect_stale_docs` 过滤 `is_stale=false`），故腐化文档被主动发现后仍长期潜伏；M17 为 top-N SWEEP 过时 doc **批量**生成重写提案，落 `doc_update_proposals` PENDING 行（＝持久审批队列），再逐项 approve/reject。①**架构裁决（Design Q「生成→队列→逐项审批」）**：generate 在审批**前**——`generate_doc_update` 只产 append-only MinIO 工件、`create_doc_pr` 只 INSERT 一行 PENDING，**均不改 doc_chunks/code/relations 源真相、不 push git**（已逐行核实），故 generate 无需审批；HITL 闸门移到管「是否采纳/待推送」（approve→`APPROVED`/reject→`REJECTED` 状态翻转）。②**新服务** `app/services/sweep_rewrite_service.py`（raw text()，镜像 staleness_sweep 约定，永不抛）：`run_sweep_rewrite`（`_SWEEP_POOL_SQL` 枚举 SWEEP+stale 关系→内联方向约定按 doc_chunk_id 去重+聚合 relation_ids（镜像 apply_stale，first-seen code 胜）→**幂等守卫** `_ACTIVE_DOC_CHUNKS_SQL` 跳过已有 active 提案(PENDING_PUSH/PENDING_MANUAL/APPROVED)的段落、FAILED/REJECTED 可重试→逐段落复用 M15 generate_doc_update+create_doc_pr）+ `list_proposals`（此表首个 SELECT，ORM 分页/过滤）+ `set_proposal_status`（UPDATE 状态翻转）。③**3 端点**（`staleness.py`，无需改 router）：`POST /v1/staleness/sweep-rewrite`+`GET /v1/staleness/proposals`+`POST /v1/staleness/proposals/{id}/decide`；新 schema（SweepRewriteRequest/ProposalItem/SweepRewriteResponse/ProposalListResponse/ProposalDecisionRequest）；config 加 `sweep_rewrite_top_n_default/max`（顺序 N 次 async LLM+同步 MinIO 故 cap 低）。④`status` 列加 `APPROVED`/`REJECTED` 值——`String(32)` 无枚举→**零迁移**（仅改 model 注释 + `alembic check` 无 diff）。**零迁移/零新表/零新依赖/6 Agent + M10–M16 运行时零改动**（M15 原语 generate_doc_update/create_doc_pr 完全复用，§7.2 @tool 计仍 13/20）。新增 `tests/test_sweep_rewrite_service.py`(17)；全量 286→**303** 测绿 + ruff 净 + `alembic check` 无 diff。**e2e（真 PG+MinIO+DeepSeek）**：种 rel5(CODE_TO_DOC) SWEEP→`run_sweep_rewrite`(scanned1/slots1/rewritten1/pending_push1，DeepSeek 真实重写「`getBalance` 返回当前账户余额的只读快照…」)→MinIO 工件 261B 可读→`list_proposals` total1（此表首个 SELECT）→`/decide` APPROVED→**幂等重跑 skipped_existing1/pending_push0**（守卫跳过、无重复）→还原（0 SWEEP/0 提案/rel5 复原）。**延后**：真写回 doc_chunks、真实 git/远程 PR（push APPROVED 行）、并发限流、前端腐化面板/审批 UI（消费本期 3 端点）、回滚关 PR 联动、整文档重写、剩 5 工具、新人引导 Agent、aggregator/Send-API 子图、Agent/Task 物化表。详见 §7.1-7.4。

> ✅ 2026-08-06 **Phase 7 Milestone 18（APPROVED 提案真写回 doc_chunks——KB 自愈闭环）完成**：补 M15→M17 旗舰弧线缺失的 **apply 半边**——approve 不再只翻 status，而是把 `rewritten_text` 写回 `doc_chunks.content` + 重算 `content_hash`/`token_count` + 置 `embedding_synced=false`（**懒重嵌入**：`resync_embeddings.py`/lifespan 循环用新 content 重嵌）+ 清 `relation_ids` 的 `is_stale`（文档已与代码一致→关系不再过时），让 M15 HITL 与 M17 SWEEP 两条产 PENDING 提案的路径在同一个 `/v1/staleness/proposals/{id}/decide` 闸门后真正落地、知识库自我修正。①**重写 `set_proposal_status`**（`sweep_rewrite_service.py`，raw text()，永不抛）：approve 时 `_PROPOSAL_FOR_WRITEBACK_SQL` 取提案 → `_WRITEBACK_DOC_SQL` 写 doc_chunks（content/hash/tokens/embedding_synced=false；updated_at 由 onupdate 自动触）→ `_CLEAR_RELATIONS_SQL` 清关系（`is_stale=false, stale_reason=NULL WHERE relation_id=ANY(:ids)`）→ `_SET_STATUS_SQL` 翻 APPROVED；**单事务 commit 一次**（任一步失败 → rollback 全回滚含 status 翻转），保证 APPROVED 恒等于「已写入」；REJECTED 仅翻转。守卫：PENDING_MANUAL（无 rewritten_text）→ `no rewrite to apply`、dangling doc_chunk（无 FK）→ `doc chunk not found`，status 均不变。②`_ACTIVE_STATUSES` 精简 `(PENDING_PUSH,PENDING_MANUAL,APPROVED)` → `(PENDING_PUSH,PENDING_MANUAL)`：已决定提案不再占 doc 槽位 → approve 后可被 M16 再次检测 / M17 再次重写 → **闭环可循环**。③新 schema `ProposalDecisionResponse`(applied/doc_chunk_id/relations_cleared) + `decide` 端点加 `response_model`。**零迁移**（全写既有列；APPROVED/REJECTED M17 已加；`alembic check` 无 diff）/零新表/零新依赖/6 Agent + M10–M17 运行时零改动（§7.2 @tool 计仍 13/20）。扩 `tests/test_sweep_rewrite_service.py`（+9，假 session 加 `UPDATE DOC_CHUNKS`/`UPDATE CHUNK_RELATIONS`/`WHERE PROPOSAL_ID` 分发键，顺序敏感置于通用 UPDATE 前）；全量 303→**312** 测绿 + ruff 净 + `alembic check` 无 diff。**e2e（真 PG，PG-only，无需 LLM/MinIO）**：插一条 PENDING 提案 → APPROVED → `doc_chunks.content` 写回 + hash/token 重算 + `embedding_synced=false` + 关系 `is_stale=false/stale_reason=NULL` + status APPROVED + `applied=True/relations_cleared=1`；REJECTED `applied=False` 不写；PENDING_MANUAL → `no rewrite to apply`；dangling → `doc chunk not found` → 还原（0 提案/关系复原/doc chunk hash+synced 复原）。**延后**：真实 git/远程 PR（push APPROVED 行填 pr_url）、eager Milvus 重嵌入（本期懒）、并发限流、前端腐化面板/审批 UI、回滚关 PR 联动、整文档重写、剩 5 工具、新人引导 Agent、aggregator/Send-API 子图、Agent/Task 物化表。详见 §7.1-7.4。

### 7.2 工具集（20 个 @tool）
- [ ] 检索类 6：vector_search_code/doc/graph、bm25_search、image_search、table_search
- [ ] 图遍历类 4：graph_traverse、get_call_chain、get_downstream_callers、get_upstream_callers
- [ ] 关联查询类 3：get_related_docs、get_affected_docs、get_javadoc
- [ ] 变更历史类 3：get_recent_changes、detect_stale_docs、mark_stale_anchors
- [ ] 精排类 1：rerank
- [ ] 生成类 2：generate_doc_update、create_doc_pr
- [ ] 图数据库类 1：neo4j_query（可选）

> **2026-07-29 裁剪**：随 Phase 6 GraphRAG 弃用，删除 `search_community_summaries`、`get_inter_community_edges`、`get_community_members`、`get_community_summary`、`get_all_communities`、`get_community_impact` 共 6 个社区相关工具（26 → 20）。

> **2026-07-29 已落地 9/20**：Milestone 2 代码理解 Agent 5 工具（`app/agent/tools/code_tools.py`：`search_code` 复用 pipeline.recall 含 rerank / `search_symbol` graph_service.search_graph_nodes / `get_call_chain` graph_service.get_call_graph / `get_related_docs` graph_service.get_code_doc_relations / `read_code` code_chunks 扩列）；Milestone 3 文档问答 Agent 3 工具（`app/agent/tools/doc_tools.py`：`search_docs` recall+`kind=="doc"` 过滤 / `read_doc` doc_chunks 扩列：全文+章节+表格/图片+出处 / `get_related_code` get_code_doc_relations 反向 doc→代码佐证）；Milestone 4 变更影响 Agent 1 工具（`code_tools.py`：`get_callers`=get_call_graph direction=CALLERS + depth + max_nodes=120 放上限，对应 §7.2「图遍历类」的上游调用方；`format_impact_callers` 按 BFS 层归类防 token 爆量）。注：graph 向量检索已废弃，结构相关仅留 `graph_traverse`/`call_graph` 遍历；`neo4j_query` 因未采用 Neo4j 实质弃用。剩余 11 工具随其余场景 Agent 逐步补齐。

> **2026-07-30 已落地 10/20**（Milestone 6 +1）：缺陷诊断 Agent 新工具 `get_recent_changes`（`code_tools.py`：查 `change_history` 表按 `chunk_id` 取最近 10 条 git 变更——change_type/commit/author/message，对应 §7.2「变更历史类」回归排查；`format_change_history` 格式化、空历史优雅降级）。注：`change_history` 仅增量同步写入，全量入库的样本库为空（工具返回提示文、`n=0` 不中断）。剩余 10 工具随其余场景 Agent 逐步补齐。

> **2026-08-04 已落地 11/20**（Milestone 11 +1）：代码审查 Agent 新工具 `get_code_metrics`（`code_tools.py`：LOC + token_count + call_graph 条件 COUNT fan-in/fan-out，量化复杂度/影响面；`formatting.py` `format_code_metrics` 带阈值提示；元数据工具只发 `agent_step` 不发 citation）。剩余 9 工具随其余场景 Agent 逐步补齐。

> **2026-08-04 已落地 12/20**（Milestone 12 +1）：测试生成 Agent 新工具 `get_existing_tests`（`code_tools.py`：解析 center→class_name 后 `class_name ILIKE '{Class}%Test'` 找现有测试类 LIMIT 8，对齐项目测试约定；**内容工具发 citation**（区别于元数据工具）；`formatting.py` `format_existing_tests`，空→JUnit5+Mockito 通用约定提示）。剩余 8 工具随其余场景 Agent 逐步补齐。

> **2026-08-05 已落地 13/20**（Milestone 13 +1）：文档维护 ReAct Agent 新工具 `detect_stale_docs`（`tools/maintain_tools.py`：解析 center→code chunk → 查 `chunk_relations` DOC_TO_CODE/CODE_TO_DOC 锚点 + 交叉 `change_history` 取 code 侧最近变更作 staleness 证据；**内容工具发 citation**；§7.2「变更历史类」）。另加 ReAct 终结控制工具 `submit_proposal`（同文件：DB 校验 relation_id 后经内部协议事件 `_proposal_captured` 把结构化提案回传 propose 节点；不发 citation）。剩余 7 工具随其余场景 Agent 逐步补齐。

> **2026-08-05 已落地 15/20**（Milestone 15 +2，**生成类**）：`generate_doc_update` + `create_doc_pr` 落地为 **apply 阶段服务动作**（`app/services/doc_maintenance_service.py`，由 `apply_stale` 直接确定性调用），**非 ReAct `@tool`**——HITL 设计：写动作必须在人工审批后执行，故不进 propose 的 ReAct 工具集（避免审批前开 PR）。`generate_doc_update` = LLM 据代码重写过时段落→写 MinIO 工件；`create_doc_pr` = 装配 PR 载荷（分支/commit/diff）+ 落 `doc_update_proposals` 表（**仅载荷、不执行真实 git**）。注：§7.2 表头「20 个 @tool」的 @tool 计仍为 13；+2 为服务层生成动作，功能项 15/20。剩余 5 项（检索类/图遍历类/关联类/精排/neo4j）随后续里程碑补。

### 7.3 8 个专业 Agent
- [x] **代码理解 Agent**（CODE_UNDERSTAND）—— Milestone 2：预置 `create_react_agent` 自动 Agent + 5 工具（search_code/search_symbol/get_call_chain/get_related_docs/read_code），意图路由后自动跑工具调用闭环
- [x] **文档问答 Agent**（DOC_ANSWER）—— Milestone 3：预置 `create_react_agent` 自动 Agent + 3 工具（search_docs/read_doc/get_related_code），意图分发表路由后自动跑工具调用闭环（文档检索→读段→拉关联代码佐证，§15.2）
- [x] **变更影响 Agent**（CHANGE_IMPACT）—— Milestone 4：预置 `create_react_agent` 自动 Agent + 5 工具（search_symbol/search_code/read_code/get_call_chain/get_callers），graph 意图路由后评估「修改 X 影响哪些调用方」（定位→确认→get_callers/get_call_chain 展开调用图，§15）；3 Agent 共享 `_base.run_scenario_agent` 骨架
- [x] **缺陷诊断 Agent**（BUG_DIAGNOSIS）—— Milestone 6：预置 `create_react_agent` 自动 Agent + 6 工具（search_symbol/search_code/read_code/get_call_chain/get_callers/**get_recent_changes**），bug 意图路由后排查「为何失败/报错根因」（定位→精读→调用上下文→回归排查→根因+修复建议）；4 Agent 共享 `_base.run_scenario_agent` 骨架
- [x] **代码审查 Agent**（CODE_REVIEW）—— Milestone 11：预置 `create_react_agent` 自动 Agent + 6 工具（search_symbol/search_code/read_code/**get_code_metrics**/get_call_chain/get_recent_changes），review 意图路由后评估代码质量/潜在问题/改进建议（定位→精读→量化度量→调用上下文→回归排查→按严重度分级问题+建议）；5 Agent 共享 `_base.run_scenario_agent` 骨架
- [x] **测试生成 Agent**（TEST_GENERATION）—— Milestone 12：预置 `create_react_agent` 自动 Agent + 5 工具（search_symbol/search_code/read_code/get_call_chain/**get_existing_tests**），test 意图路由后为方法生成 JUnit 单元测试（定位→精读→调用链看依赖→对齐项目测试约定→生成正常+边界+异常用例）；6 Agent 共享 `_base.run_scenario_agent` 骨架。**踩坑**：模块文件/节点函数均以 `test_` 开头致 pytest 误收集 → 测试用模块属性 `tg.test_generation` + `pyproject testpaths=["tests"]` 双修
- [x] **文档维护 Agent**（DOC_MAINTAIN，完整 ReAct）—— Milestone 10（HITL 桩）+ Milestone 13（升级为完整 ReAct）+ Milestone 15（apply 写动作闭环）：propose 节点改为 `create_react_agent` 自主排查（定位→`detect_stale_docs` 找锚点+staleness 证据→read_code/read_doc 精读两侧→get_recent_changes→`submit_proposal` 结构化多锚点提案），仍走主图 `confirm`（interrupt）→apply/reject HITL 闸门；**M15：apply 批准后据代码 LLM 重写过时段落（generate_doc_update）→ 工件写回 MinIO → 装配 PR 提案落 `doc_update_proposals` 表（create_doc_pr，仅载荷不执行 git；按 doc_chunk_id 去重聚合 relation_ids）**。**写动作 Agent，opt-in 不接 intent**（显式 `agent_type=DOC_MAINTAIN` 触发）。无 LLM key 降级到 M10 硬编码排查
- [ ] 新人引导（按 `mcp 工具集合` 工具子集映射）

> **2026-07-29 裁剪**：删除「全局问答」Agent（依赖社区摘要），9 → 8。

### 7.4 触发与面板
- [ ] 触发器：用户提问（路由）、PR Webhook（审查+影响+测试）、代码变更（文档维护）、告警（缺陷诊断）
- [x] **agent_step 可观测性（Milestone 5）**：工具调用轨迹持久化（`retrieval_logs.agent_steps` JSONB）+ `GET /retrieval` 回放 `agent` 段 + 前端捕获/渲染（`RetrievalDrawer`「Agent 推理轨迹」段 + `RetrievalSummary` 步骤 pill）——3 个场景 Agent 的工具调用过程现已可见、可回放
- [x] **HITL 超时 + 线程状态可观测（Milestone 14）**：超时未审批的中断自动 `expired`（+清其 thread checkpoint）；`GET /conversations/{id}/state` 查线程执行态（是否待审批/提案/已等多久）；`POST /chat/continue` 通用续跑（断流恢复）；`MessageItem.status` 暴露 + 历史过滤占位
- [x] **腐化面板 + 提案审批 UI（Milestone 19）**：`/staleness` 页消费 M16–M18 的 4 个 `/v1/staleness/*` 端点——过时关系报告（KPI + 来源分解 + 最近发现）+ 提案审批队列（status 过滤）+ 巡检 top-N 批量生成提案 + 审批 Drawer（`<ReactMarkdown>` 原文/重写两栏预览 + Popconfirm 通过写回/驳回）；后端 `ProposalItem` 暴露 `rewritten_text`/`original_text`（既有列，零迁移）
- [x] **eager Milvus 重嵌入（Milestone 20）**：补 M18 写回后向量检索仍返回旧段落的延迟——`set_proposal_status` approve 写回 `doc_chunks.content` 后 **post-commit best-effort 即时重嵌入**该 chunk（复用 `indexing.index_chunks_to_milvus`，经 `asyncio.to_thread` 全位置参封装异步 session 调同步原语；成功翻 `embedding_synced=true`，失败/无密钥降级留懒 flag 由 `resync_pending_embeddings` 兜底，**永不抛/永不染 error**——否则 `/decide` 误判 400 掩盖写回）；降级三态 `reembed_status`(synced/failed/lazy) 入 `ProposalDecisionResponse` + `eager_reembed_enabled` opt-out kill-switch；DOC_MAINTAIN 反应式 + SWEEP 主动式两路均汇此单 hook（`apply_stale` 不写 content）；零迁移/零新依赖；319 测
- [x] **真实 git/PR 落地（Milestone 21）**：补 M10→M20 旗舰弧线最后一步——`set_proposal_status` approve 写回 KB+重嵌后 **post-commit best-effort 真 git**：读磁盘文档→`original_text`→`rewritten_text` 替换→**隔离 `git worktree` 建分支+提交**（不变异主工作区）→（`doc_git_push_enabled` 默认关 opt-in）推送→回填 `commit_sha`/`pr_url`、状态翻 `PUSHED`/`COMMITTED`（失败→`PUSH_FAILED`，KB 已写回），**永不抛/永不染 error**（同 M20）；换掉 `close_open_doc_pr_for` 桩为真实现（按 `source_commit` 删分支、翻 `CLOSED_BY_ROLLBACK`，契约不变自开同步 session）。`sync_git.py` 扩写原语（worktree add/remove、commit、push、branch delete，沿用 `_run`+`_run_try` best-effort）；编排放新 `doc_pr_service.py`；splice 精确唯一优先 + CRLF 归一化兜底 + 否则优雅失败（`doc_chunks` 无 offset）；一条迁移加 `commit_sha` 列 + `source_commit` 在 `create_doc_pr` 经 `git_head` 捕获 base（回滚匹配键）；status 加 PUSHED/COMMITTED/PUSH_FAILED（无枚举零迁移）；config 五开关；前端据 git_status 提示 + 抽屉分支/commit/PR 链接；337 测
- [ ] Agent 面板模块 5 接口（列表/任务历史/详情/手动触发/反馈）
- [ ] 前端 Agent 面板页（状态卡片 + 最近任务）

---

## Phase 8 — 回滚 / 监控 / LTR

### 8.1 代码回滚（§18）
- [x] 2026-07-28　`pipeline/sync_incremental.py`：git diff → ADDED/MODIFIED/DELETED（code 按锚点 per-chunk，doc 文件级）+ Milvus 孤儿清理；`pipeline/sync_rollback.py`：`ROLLBACK`/`RESTORED` 识别（查 change_history）+ §18 四.2 恢复（关系/锚点/doc stale/调用图标记翻回；Milvus/ES 已由 ingest 步骤重写）✅
- [x] 2026-07-28　`pipeline/sync_soft_delete.py`：§6.4 DELETED 软删除级联（is_deleted/stale/inactive 标记 + Milvus/ES 硬删，行保留供 RESTORED 重激活）✅
- [x] 2026-07-28　`rollback_history`/`change_history`/`sync_tasks` 表（含 §18 回滚字段 + PR 字段）—— **初始迁移已建，无需新迁移**；`change_history` 首次有写入 ✅
- [x] 2026-07-28　`services/sync_service.run_sync` 编排（FULL 复用 ingest_repo / INCREMENTAL 无游标回退 FULL / savepoint 隔离 per-file 失败 / change_details JSONB）+ CLI `scripts/sync_repo.py` ✅
- [x] 2026-07-28　回滚详情接口（`GET /sync/tasks/{id}` change_details、`/sync/rollbacks`）✅；前端同步管理页回滚高亮详情 ✅（Phase 8.4，见下）
- [x] 2026-07-28　`milvus_client.delete_vectors` + `indexing.delete_chunks_from_milvus`（chunk_id 即 Milvus 主键，按 PK 删）✅
- [x] 2026-07-28　**真实 git 仓库端到端实测**（`data/repo/gitdemo`，>200 行方法级文件）：FULL(C1)→改+删(C2 INCREMENTAL)→`git revert`(C3 INCREMENTAL) 全链路 + 6 个 `/sync/*` 接口全通 ✅。实测：C1 FULL 建锚点/调用图/Milvus/ES；C2 `added=1/modified=1/deleted=19`（transfer+withdraw 改、getBalance per-chunk 硬删、Profile 整文件软删 §6.4 级联 + Milvus/ES 清理）；C3 `rollbacks=1`（RESTORED 19、rollback_history 审计行、§18 ⑤⑦ Milvus/ES 重 embed、change_history 全生命周期 `DELETED@C2→RESTORED@C3 rb_related`）。**修复 2 个实测发现的崩溃 bug（见下）**。
- [x] 2026-07-28　**前端同步管理页（Phase 8.4）**：`api/sync.ts`（6 接口 typed client）+ `pages/SyncPage.tsx`（状态统计卡 + 任务表[变更汇总+回滚高亮+详情抽屉 change_details/rollback_detail/errors] + 回滚记录表 + 触发同步弹窗 FULL/INCREMENTAL/目标提交 + 20s 轮询）+ App.tsx 路由替换；`pnpm typecheck` + `pnpm build` 通过 ✅

> **2026-07-28 端到端实测修复与发现**（sync_service.py，均为真实 git 仓库实测暴露、单测未覆盖）：
> - **Bug 1（已修）** `_relations_total`：`relations.build_all` 实际返回 `anchors` 为**嵌套 dict**（`{relations,anchor_mappings,unmatched_anchors}`），旧代码 `total = rel.get("anchors",0)` 取到 dict 再 `+= call_edges` → `dict+int TypeError`，FULL 同步在 `_run_full` 内崩溃。改为按 dict/int 两种形状防御求和。**原单测 `test_run_full_assembles_counts`/`test_relations_total` mock 的 anchors 是 int（错误形状），故未发现**——已修正为真实形状 + 补回归。
> - **Bug 2（已修）** `run_sync` 错误处理：except 块用 `type(e).__name__`，但 `type` 是 `run_sync(*, type:str)` 形参被遮蔽 → `type(e)`=`"FULL"(e)` 抛 `'str' object is not callable`，**任何同步失败都崩在错误处理本身**，FAILED 状态/error_message 永不落库、任务卡死 RUNNING。改用 `e.__class__.__name__`。补 `test_run_sync_marks_failed_without_crashing_handler` 回归。
> - **发现 3（已修复，2026-07-29）** **§18 ①②③④ 关系/锚点/调用图回滚恢复失效**：前向 `soft_delete_file` 对 `chunk_relations.is_stale`/`anchor_mappings.is_active`/`call_graph.is_deleted` 打标记（供回滚翻回），但回滚时整文件走 `apply_added_or_modified`→`ingest_java_file`→`replace_chunks`→`clear_code_chunk_refs`（`db/references.py`）会**硬删**这些标记行，导致随后 `apply_rollback_restore` 翻标记时已无行可翻 → `relations_restored/anchors_restored` 恒为 0，回滚后知识图谱为空至下一次 FULL。**修复**：`sync_service._run_incremental` 检测到 `rollback_rows` 非空时调 `relations.build_all` 全量重建关联（仅链活跃 chunk，终态正确）；`task.relations_updated` 计入、`graph_update_status=COMPLETED`。补 2 回归单测（回滚→build_all 调用 / 无回滚→不重建）。**e2e 复测**：gitdemo FULL(C1 rels=4)→INC(C2)→revert INC(C3) 后 **rels=4 / anchors=2 / call_graph=3**（修复前为 0/0/0），知识图谱随回滚完整恢复。注：`rollback_history.relations_restored` 仍记 0（恢复经 build_all，非 flag-flip），实际关系数见 chunk_relations 计数——属计数口径，非缺陷。

### 8.2 同步管理模块（6 接口）
- [x] 2026-07-28　`GET /sync/status`、`/sync/tasks`、`/sync/rollbacks`、`/sync/tasks/{id}`、`/sync/change-history/{chunk_id}` ✅
- [x] 2026-07-28　`POST /sync/trigger`（asyncio.to_thread + 位置参数包装调 run_sync）✅

### 8.3 定时任务（§13.3）
- [ ] 每小时：补偿同步（`embedding_synced=FALSE`）
- [ ] 每日：全量 GNN 重算 + 文档腐化检测
- [ ] 每周：社区检测重算 + 索引优化（Milvus compact / PG vacuum / ES optimize）

### 8.4 监控（4 接口）+ 前端
- [ ] `GET /monitor/retrieval-perf`、`/resources`、`/api-usage`、`/index-stats`
- [ ] 前端系统监控页（检索性能漏斗 + GPU 显存 + API 用量 + 索引统计）

### 8.5 WebSocket 实时推送
- [ ] `WS /ws/v1/events`：sync.* / rollback.* / agent.* / doc.stale_detected / system.gpu_alert / document.*

### 8.6 LTR（持续）
- [ ] `retrieval_logs` 反馈采集 → 特征工程
- [ ] LambdaMART（LightGBM/XGBoost）训练 → A/B 调权

---

## 横切：全局搜索 / 设置 / 全局快捷搜索（穿插于各 Phase）

- [ ] `GET /api/v1/search/global`（⌘K，含 image/table）
- [ ] `GET /api/v1/search/docs`
- [ ] `GET/PUT /api/v1/settings`
- [ ] `POST /api/v1/settings/test-connection`
- [ ] 前端 ⌘K 全局快捷搜索

---

## 横切：评测与质量（持续）

- [ ] 评测集（~50–100 标注问题）
- [ ] Recall@K / MRR / NDCG 脚本
- [ ] 后端 pytest（单元 + 集成，dockerized）
- [ ] 前端 Vitest + Playwright
- [ ] CI 全绿

---

## 进度统计（手动维护）

| Phase | 任务数 | 已完成 | 状态 |
| --- | --- | --- | --- |
| Phase 0 基础设施 | 11 | 10 | ✅ 完成（infra 在 Docker，应用本地；CI 待补） |
| Phase 1 基础 RAG | 36 | 34 | ✅ **基本完成**（解析/切片/关联/调用图 + 三路召回 + RRF + 精排 + LLM流式 + SSE + 会话持久化 + 前端富展示）；右侧常驻上下文面板/编辑器级代码高亮为后续增强 |
| Phase 1.5 多模态 | 22 | 22 | ✅ **Phase 1.5 全部完成**（1.5a 文本/1.5b 图片 OCR/1.5c 表格结构化/1.5d 文档管理 API+前端/1.5e 富媒体引用）；PDF/Word/txt 全格式解析、表格结构化、图片 OCR、上传管理、聊天内图表渲染；94 单测 + ruff + 多 e2e 通过；仅余 DOC_CONTAINS_IMAGE 关系 / 全局搜索&WS 支持 image/table（增强项）|
| Phase 2 精排 | 6 | 4 | ✅ 核心完成（单阶段精排 v2-m3 + RRF 融合 + 前端漏斗；粗排两阶段待本地 model_server；A/B 评测待） |
| Phase 3 BM25+RRF | 5 | 4 | RRF 正式融合 ✅（含 6 单测）+ Stage0 LLM 查询改写 ✅；评测待 |
| Phase 4 图遍历 | 6 | 6 | ✅ **完成**（graph_traverse 召回前置 + RRF 图权重 1.2 + **知识图谱 4 接口** + **前端 Cytoscape 图谱页 + 右侧上下文面板**；7 单测 + e2e + typecheck/build）；召回 +20% 评测待 |
| Phase 5 GNN | 8 | 0 | ❌ **已弃用**（图向量移除；保留图遍历，见上） |
| Phase 6 GraphRAG | 6 | 0 | ❌ **已弃用**（2026-07-29，社区摘要功能整体移除：删表/列/模型 + 前端社区总览 + Phase 7 社区工具/全局问答 Agent；见上） |
| Phase 7 Agent | 20 | 15 | **Milestone 1（地基）+ M2（代码理解 Agent）+ M3（文档问答 Agent + 多 Agent 分发表）+ M4（变更影响 Agent + `_base` 共享骨架）+ M5（Agent 步骤可观测性）+ M6（缺陷诊断 Agent + get_recent_changes）+ M7（Agent 面板 `/agents` + 聚合统计 API，零新表按需聚合 retrieval_logs）+ M8（AsyncPostgresSaver 持久化——断点续跑地基）+ M9（跨轮对话记忆——从 chat_messages 载入历史注入 build_messages/场景 Agent 种子，修复 langgraph/legacy 两路跨轮失忆；`conversation_history_turns` 开关，0=禁用）完成**：地基=LangGraph 主图（`RAG_ENGINE` 开关，默认 legacy）+ state/nodes/checkpointer(MemorySaver) + SSE 适配器；M2=`query_analysis` 意图分类 + `router`，**代码理解 Agent 用预置 `create_react_agent` 自动跑 5 工具闭环**；M3=**`router` 重构为意图→Agent 分发表**（双字典，加 Agent=两行）+ **文档问答 Agent**（3 工具）；M4=**抽 `_base.run_scenario_agent`**（4 Agent 共享骨架）+ **变更影响 Agent**（CHANGE_IMPACT，5 工具含新 `get_callers`，graph 意图路由）；M5=**agent_step 持久化（`retrieval_logs.agent_steps`）+ `GET /retrieval` 回放 `agent` 段 + 前端捕获/渲染**，补齐 Agent 不可观测缺口；M6=**缺陷诊断 Agent**（BUG_DIAGNOSIS，6 工具含新 `get_recent_changes` 查 change_history 回归排查，bug 意图路由）+ **前端下拉 bug 修复**（DOC_QA→DOC_ANSWER、删 GLOBAL_QA）。token 经回调桥接 SSE、引用经事件累积、异常 `_degrade` 兜底。e2e：代码意图 6 步、文档意图 7 步、变更影响意图 6 步、GET /retrieval 回放 agent 4 步轨迹、单 retrieval 无降级、流式作答落库。全量 211 项绿、ruff 净。M7=**Agent 面板 `/agents`**（`GET /agents/stats`+`/runs`，零新表按需聚合 retrieval_logs：mode='agent' 成功信号 / agent_steps 轨迹 / user_feedback 满意度 / 降级=agent_steps 非空且 mode 缺失；LEFT JOIN chat_messages 恢复降级 run 标签；jsonb_typeof 守护标量行），前端 `<AgentsPage/>`（4 KPI + 各 Agent 明细表 + 最近运行表 + 20s 轮询）。M8=**AsyncPostgresSaver 持久化**（`LANGGRAPH_CHECKPOINT=postgres` 用 AsyncPostgresSaver 替 MemorySaver，thread 状态持久化 PG 四表跨重启存活；`postgres_dsn` 裸 DSN + `from_conn_string` autocommit 建 CONCURRENTLY 索引 + alembic `include_object` 排除四表；踩两 Windows 坑：psycopg 需 SelectorEventLoop + uvicorn 0.51 硬编码 Proactor 故覆盖 `get_loop_factory`）。**延后**：第 7+ 场景 Agent（文档维护完整 ReAct/新人引导）、8 工具、aggregator/Send-API 子图、通用 astream(None) 续跑 API、HITL 图级超时、checkpoint 老化清理、Agent/Task 物化汇总表（M7 按需聚合兑现 `/agents` 面板；M9 跨轮记忆从 chat_messages 注入 build_messages/Agent 种子，两路 LLM 不再跨轮失忆；M10 HITL 人在回路——主图节点 interrupt()/Command(resume) + 适配器中断检测 + `/v1/chat/resume` + 审批 Modal，interrupt 经 M8 checkpoint 跨重启续跑；M11 代码审查 Agent——第 5 场景 Agent + get_code_metrics(LOC/fan-in/out) + review 意图；M12 测试生成 Agent——第 6 场景 Agent + get_existing_tests(对齐项目测试约定/发 citation) + test 意图(不收裸 test/mock 防误判) + testpaths=["tests"] 修 pytest 收集碰撞）；M13 文档维护完整 ReAct——propose 升级为 create_react_agent（detect_stale_docs 找锚点+staleness 证据 / submit_proposal 经 _proposal_captured 内部事件回传结构化多锚点提案；泛化 apply 循环标记；opt-in 不接 intent；无 LLM 降级 M10 桩；agent_step 轨迹可观测）；M14 运营加固·持久化地基——checkpoint 老化清理（整 thread 过期：最新 checkpoint ts 超保留期才整删三表/活跃线程保留，独立 psycopg 连接隔离 saver Lock）+ HITL 图级超时（lifespan 维护循环把超时 interrupted→expired+清其 thread checkpoint）+ GET /conversations/{id}/state 线程态 + POST /chat/continue astream(None) 断流恢复（snap.next 闸门避 EmptyInputError）+ MessageItem.status 暴露 + 历史过滤占位 + /resume 409；零迁移/零新依赖；262 测；M15 DOC_MAINTAIN 写动作闭环——apply 扩为 generate_doc_update(LLM 据代码重写过时段落→写回 MinIO)+create_doc_pr(装配 PR 载荷/落 doc_update_proposals 表 PENDING_PUSH，仅载荷不执行 git；无 LLM→PENDING_MANUAL；按 doc_chunk_id 去重聚合 relation_ids)；新表+迁移；272 测；M16 主动腐化巡检——lifespan 后台循环定时枚举非过时 DOC↔CODE 关系，据 change_history 启发式规则（代码侧 MODIFIED/DELETED 晚于关系 updated_at）自动标 is_stale（stale_reason=SWEEP:…，仿 soft_delete 非-HITL 标记；重写仍走 M15 HITL 闸门）；新服务 staleness_sweep_service + GET /v1/staleness/report + config 三开关；仅 gate staleness_sweep_enabled（不要求 langgraph，巡检纯 PG）；零迁移/零新表/零新依赖；286 测；M17 SWEEP 批量重写——为 top-N SWEEP 过时 doc 批量 generate_doc_update+create_doc_pr 落 doc_update_proposals PENDING 审批队列 + 逐项 approve→APPROVED/reject→REJECTED 状态翻转（零迁移）；幂等守卫跳过已有 active 提案的段落（FAILED/REJECTED 可重试）；新服务 sweep_rewrite_service + 3 端点（/v1/staleness sweep-rewrite/proposals/decide，此表首个 SELECT）；架构裁决 generate 在审批前（草稿非破坏、不改源真相不 push git）；零迁移/零新表/零依赖；312 测（社区 6 工具 + 全局问答 Agent 已随 Phase 6 删，26→20 / 9→8）；M18 APPROVED 真写回 doc_chunks——`set_proposal_status` approve 时写回 `rewritten_text`→`doc_chunks.content` + 重算 hash/token + 置 `embedding_synced=false` 懒重嵌入 + 清 `relation_ids` 的 `is_stale`（单事务原子，APPROVED 恒等于「已写入」）；`_ACTIVE_STATUSES` 精简(去 APPROVED)让闭环可循环；新 `ProposalDecisionResponse` + decide `response_model`；零迁移/零新表/零依赖；**M19 前端腐化面板+审批 UI**——新页 `StalenessPage`（KPI/最近过时发现/提案审批队列 + 巡检 top-N Modal + 审批 Drawer：`<ReactMarkdown>` 原文/重写两栏预览 + Popconfirm 通过写回/驳回）+ `api/staleness.ts` + `/staleness` 路由 + 管理组「腐化审批」导航；后端 `ProposalItem`+`_proposal_to_dict` 暴露 `rewritten_text`/`original_text`（既有列，零迁移）；客户端分页+status 过滤+20s 轮询（不经 store）；不引新依赖；313 测；**M20 eager Milvus 重嵌入**——`set_proposal_status` approve 写回 doc_chunks 后 **post-commit best-effort 即时重嵌入**该 chunk（复用 `indexing.index_chunks_to_milvus` 经 `asyncio.to_thread` 全位置参封装异步调同步原语；成功翻 `embedding_synced=true`，失败/无密钥降级留懒 flag 由 resync 兜底，**永不抛/永不染 error** 否则 /decide 误判 400）；降级三态 `reembed_status`(synced/failed/lazy) 入 `ProposalDecisionResponse` + `eager_reembed_enabled` opt-out kill-switch；DOC_MAINTAIN 反应式 + SWEEP 主动式两路均汇此单 hook（`apply_stale` 不写 content）；零迁移/零新表/零新依赖/M10–M19 运行时零改动；**319 测**；**M21 真实 git/PR 落地**——`set_proposal_status` approve 写回 KB+重嵌后 post-commit best-effort 真 git：隔离 `git worktree` 建分支+提交（不变异主工作区）+可选推送→回填 `commit_sha`/`pr_url`、状态翻 `PUSHED`/`COMMITTED`（失败→`PUSH_FAILED`，KB 已写回），永不抛/永不染 error；`sync_git.py` 扩写原语（worktree/commit/push/branch delete）+新 `doc_pr_service.py` 编排（splice 精确优先+CRLF 归一化兜底+否则优雅失败）+closer 换真实现（按 source_commit 删分支翻 CLOSED_BY_ROLLBACK，删 stub）；一条迁移加 `commit_sha`（区别 source_commit=base 经 git_head 捕获作回滚匹配键）；config 五开关（push opt-in 默认关）；**337 测** |
| Phase 8 回滚/监控/LTR | 16 | 10 | **8.1/8.2 后端 + 端到端实测 + 前端同步页完成**（git 增量同步 ADDED/MODIFIED/DELETED + §18 回滚检测/恢复 + 6 sync 接口 + CLI + 真实 git 仓 e2e + `SyncPage.tsx`）；实测修复 2 崩溃 bug + **§18 关系恢复失效已修**（回滚时 build_all 重建）；8.3 定时/8.4 监控前端/8.5 WS/8.6 LTR 待 |

> **当前进度（2026-07-27）**：**Phase 0 ✅ + Phase 1（核心闭环 + 会话 API + 前端富展示）✅ + Phase 2 精排 + Phase 3 RRF + Stage0 改写 ✅**。**嵌入双框架（unified 全 BGE-M3 / dual 方案一 CodeBERT+BGE-M3）✅；图向量（Phase5/路径C）已弃用，保留图遍历**。
> 已跑通端到端闭环：代码/文档解析 → 切片 → 锚点关联/调用图 → **ES BM25 + Milvus 向量(BGE-M3 API) + 图遍历** 三路召回 → **RRF 融合(k=60)** → **bge-reranker-v2-m3 精排** → DeepSeek 流式生成 → SSE → 前端问答页（**会话侧边栏 + 引用卡片 + 检索漏斗 + 检索详情抽屉 + 追问建议 + 消息反馈**）。
> 实测精排：`向量7 + 词法4 + 图遍历1 → RRF7 → 精排7`，relevance 分 0.99/0.66/…；会话/消息/检索详情/反馈/追问 API 全通（conversations/chat_messages 落库 + retrieval_logs 漏斗）。
> **架构**：infra 在 Docker，后端/前端/model_server 本地跑（嵌入+精排均走 API，不用 GPU）。
> **下一步候选**：~~增量更新(Git) + 回滚(§18)~~ ✅ + ~~真实 git 仓端到端实测~~ ✅ + ~~同步管理前端页（Phase 8.4）~~ ✅ + ~~修复 §18 关系回滚恢复失效~~ ✅（2026-07-29，回滚时 build_all 重建）/ ~~Phase 1.5 多模态（a/b/c/d/e 全完成）~~ ✅ / ~~Phase 4 知识图谱 API + 前端（Cytoscape 图谱页 + 右侧上下文面板）~~ ✅（2026-07-29）/ ~~右侧常驻上下文面板~~ ✅（随 Phase 4 接上）/ 编辑器级代码高亮 / dual 框架实测（需本地 model_server 跑 CodeBERT + 重新入库）/ Celery 任务化（补偿逻辑已纯函数化，beat 可薄封装）/ 同步 8.3 定时 cron + 8.5 WebSocket 事件推送 / Phase 7 多 Agent（社区/全局问答已移除）。
>
> **关键修复记录**（供排错参考）：
> - elasticsearch-py 9.x 与 ES8.11 服务器不兼容（Accept 版本报错）→ 锁 8.x
> - Milvus 须配 `MINIO_ACCESS_KEY_ID/SECRET` 匹配 MinIO 凭据，否则 crashloop
> - `asyncio.to_thread` 传 keyword-only 的 `top_k` 会 TypeError 被吞 → 改位置参数（es_client/milvus_client）
> - 中文 Windows GBK：`alembic.ini` 纯英文；脚本输出 emoji 需 `sys.stdout.reconfigure(utf-8)`
> - etcd 用 docker.io 的 `bitnami/etcd`（原 quay.io 不走国内加速）；前端容器须 COPY `pnpm-workspace.yaml`（否则 esbuild 构建被当错误）
> - **硅基流动 `/rerank` 无 `bge-reranker-base`**（HTTP 400 Model does not exist），只有 `BAAI/bge-reranker-v2-m3` → 粗排模型留空走单阶段精排；精排每阶段须独立 try/except，否则粗排失败会连带吞掉精排
> - **CodeBERT 不是托管 API**（硅基流动无此模型）→ 框架二（dual）的代码侧 CodeBERT 经本地 `model_server`（transformers）提供 768d；`EMBEDDING_STRATEGY=unified` 时完全不需要 model_server，主链路永不因 CodeBERT 未装而断（代码向量路自动降级为空）
> - **切换 embedding_strategy 后须重新入库**：unified 写 `coderag_vectors`，dual 写 `code_vectors`/`doc_vectors`；旧 collection 数据不自动迁移
