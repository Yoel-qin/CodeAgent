结合上面的系统架构设计文档回答下面的面试问题，结合该问题有什么坑和可以改进的地方？

为什么没人用 RAG 做代码托管和实时检索？

State管理和Checkpoint机制怎么实现的？多个agent同时跑的时候，状态竞争怎么避免？State的全局管理是怎么做的？

长期记忆和短期记忆？短期记忆里会存储function call的内容吗？如果不放进短期记忆的话会有什么问题？

多 Agent 或多异步任务场景下，怎么防止上下文污染

做 RAG 问答，命中率有没有提升？
召回和重排有没有对比？
回答延迟从多少降到多少？
幻觉率怎么测？
Token 成本有没有压？
上线后遇到过什么 badcase，怎么修？

工具调用超时怎么办？
多个工具结果冲突怎么办？
用户问题很模糊，系统怎么追问？
多轮对话越聊越偏，怎么拉回来？
模型回答不稳定，怎么评估和兜底？

为什么要静态长期记忆和动态长期记忆？每一轮对话触发一次长期记忆存储，有没有出现用户长期记忆快速积累，存的过多？

大模型怎么决定长期记忆是否需要召回？

压缩机制是做的？上下文窗口总token多大？触发上限为什么选这个值？

为什么技术选型需要GraphRAG？和纯向量检索的区别在哪？解决了什么问题？

跨会话记忆

为什么用LangGraph构架状态图？如何定义循环终止条件防止Agent陷入逻辑死循环？

如果工具调用超时或返回空值，如何设计prompt让agent反馈用户

检索出来的历史信息，语义相关性太久，能直接用吗？

